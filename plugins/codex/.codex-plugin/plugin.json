{
  "name": "tugite",
  "version": "7.0.0",
  "description": "Use how-it for explicit Interactive requested-output workflows, explorer-this for explicit exploration, and impl-lead, plan-agent, plan-interactive, review-refine, code-review, and test-report with internal skills, Implementation Unit workers, reviewers, and advisors while the parent Codex agent owns planning, QA, and final verification.",
  "author": {
    "name": "akitanabe"
  },
  "repository": "https://github.com/akitanabe/tugite",
  "license": "MIT",
  "keywords": [
    "multi-agent",
    "software-development",
    "planning",
    "delegation",
    "code-review",
    "tdd"
  ],
  "skills": "./skills/",
  "interface": {
    "displayName": "Tugite",
    "shortDescription": "Install agents and delegate with parent QA.",
    "longDescription": "Codex skills for the public workflows how-it, explorer-this, impl-lead, plan-agent, plan-interactive, review-refine, code-review, and test-report, their internal skills, Implementation Unit workers, reviewers, and advisors. How-it and explorer-this are explicit-only, keep one task-local Local Model, and connect directly to requested output; how-it uses Interactive Model Construction for unresolved ways to proceed or make the request viable, while explorer-this uses Agentic Model Construction first and composes Interactive Model Construction only for unresolved Human-owned material gaps, leaving non-Human material gaps as qualified stops. Code-review also applies when a user clearly wants a Tugite code-review of a change set, and reports evidence-checked findings without fixing or adjudicating. Test-report also applies when a user clearly wants to understand a specified set of tests, and observes them as a Verification Topology without editing, running, or evaluating tests. The parent Codex agent performs parent QA and keeps final quality responsibility.",
    "developerName": "akitanabe",
    "category": "Productivity",
    "capabilities": [
      "Write"
    ],
    "defaultPrompt": [
      "Use $how-it only for an explicit public workflow; use Interactive Model Construction to build how to proceed or make the request viable, keep one task-local Local Model, and connect directly to the requested output.",
      "Use $explorer-this only for explicit public exploration requests; keep one task-local Local Model, use Agentic Model Construction first, compose Interactive Model Construction only for unresolved Human-owned material gaps, and connect the result directly to the requested output.",
      "Use $test-report when explicitly requested or when the user clearly wants to understand a specified set of tests; observe them as a Verification Topology and report observed gaps without editing, running, or evaluating tests.",
      "Use $plan-agent only when explicitly requested to establish Agent-side planning direction and return the common Planning Core result without starting implementation.",
      "Use $plan-interactive only when explicitly requested to establish Human-confirmed planning direction and return the common Planning Core result without starting implementation.",
      "Use $review-refine to review and coherently refine a verified artifact snapshot, then return converged or incomplete with the latest verified candidate.",
      "Use $code-review when explicitly requested or when the user clearly wants a Tugite code-review of a change set; capture an immutable snapshot, route specialist reviewers, and report evidence-checked findings without fixing, adjudicating, or writing to the review target. Do not start during impl-lead or plan-family public workflows.",
      "Use $impl-lead only when explicitly requested to normalize implementation work into Implementation Units, then orchestrate implementation, Parent QA, acceptance, final verification, and safe integration.",
      "Use $install-custom-agents to check, install, or update the bundled custom agents."
    ]
  }
}
