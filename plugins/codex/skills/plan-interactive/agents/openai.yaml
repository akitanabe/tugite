interface:
  display_name: "Plan Interactive"
  short_description: "Draft a human-directed plan through gate and fixed review"
  default_prompt: "Use $plan-interactive only when explicitly requested to draft a human-directed plan, then gate and review it without starting implementation."
policy:
  allow_implicit_invocation: false
