interface:
  display_name: "Impl Lead"
  short_description: "Delegate implementation with parent QA"
  default_prompt: "Use $impl-lead to split implementation work, delegate safely, and review the returned diff."
