interface:
  display_name: "Impl Lead"
  short_description: "Normalize implementation work before execution"
  default_prompt: "Use $impl-lead only when explicitly requested to normalize implementation work into pre-execution Implementation Units, then return incomplete without starting execution."
policy:
  allow_implicit_invocation: false
