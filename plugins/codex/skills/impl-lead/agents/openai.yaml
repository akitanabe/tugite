interface:
  display_name: "Impl Lead"
  short_description: "Implement scoped work, run QA, and complete acceptance"
  default_prompt: "Use $impl-lead only when explicitly requested to normalize implementation work into Implementation Units, then orchestrate implementation, Parent QA, acceptance, final verification, and safe integration."
policy:
  allow_implicit_invocation: false
