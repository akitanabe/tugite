interface:
  display_name: "Internal Proposal"
  short_description: "Draft or refine a plan candidate only inside a proposal-family public workflow parent"
  default_prompt: "Use $proposal only inside the same context of a proposal-family public workflow parent, following injected authority and authority_constraints; return a candidate snapshot or stop-incomplete without selecting downstream work."
policy:
  allow_implicit_invocation: true
