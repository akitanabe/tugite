interface:
  display_name: "Internal Proposal"
  short_description: "Draft and bound a plan candidate only inside plan-craft"
  default_prompt: "Use $proposal only as the internal plan-craft前段; return a candidate snapshot or stop-incomplete and do not start review-loop."
policy:
  allow_implicit_invocation: true
