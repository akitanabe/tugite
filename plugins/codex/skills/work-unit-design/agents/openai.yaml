interface:
  display_name: "Internal Work Unit Design"
  short_description: "Design candidate Work Units only inside plan-craft or impl-lead"
  default_prompt: "Use $work-unit-design only inside plan-craft or impl-lead as an internal step; do not invoke it directly."
policy:
  allow_implicit_invocation: true
