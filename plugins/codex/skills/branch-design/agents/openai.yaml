interface:
  display_name: "Branch Design"
  short_description: "Normalize a plan into a delegatable Branch Plan"
  default_prompt: "Use $branch-design to normalize this plan into a delegatable Branch Plan without starting delegation."
