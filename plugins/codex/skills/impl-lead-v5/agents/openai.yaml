interface:
  display_name: "Impl Lead v5"
  short_description: "Run bounded implementation loops for one or more Work Units with parent QA"
  default_prompt: "Use $impl-lead-v5 to normalize one or more Work Units, execute them safely, and accept or stop-incomplete with evidence."
policy:
  allow_implicit_invocation: false
