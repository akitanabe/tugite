interface:
  display_name: "Impl Lead v5"
  short_description: "Run one bounded implementation loop with parent QA"
  default_prompt: "Use $impl-lead-v5 to intake one bounded Work Unit, implement it safely, and accept or stop-incomplete with evidence."
policy:
  allow_implicit_invocation: false
