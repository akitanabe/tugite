interface:
  display_name: "Impl Lead v5"
  short_description: "Run bounded Work Unit loops with optional risk-directed review and parent QA"
  default_prompt: "Use $impl-lead-v5 to normalize one or more Work Units, select review only for concrete decision-changing risk, execute safely, and accept or stop-incomplete with evidence."
policy:
  allow_implicit_invocation: false
