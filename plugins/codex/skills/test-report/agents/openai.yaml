interface:
  display_name: "Test Report"
  short_description: "Reconstruct a static Verification Topology without evaluation"
  default_prompt: "Use $test-report to statically observe the requested test scope, prepare candidate Data for the shared Verification Topology Method, preserve the returned many-to-many relation and observation limits, and report without executing, evaluating, or remediating tests."
policy:
  allow_implicit_invocation: true
