<!-- Generated from shared/. Do not edit directly. -->

# 過剰実装のプラン審査

`over-engineering-reviewer` をプラン入力モードで起動し、プランが新規に導入しようとする要素のうち、
取り除いても AC と明示された制約を満たせるものを実装前に検出する規約を定める。読み替えの正本は
`over-engineering-reviewer` 自身の `## プラン入力モード` 節にあり、この文書は起動契約だけを定める。

## 目次

- 起動契約
- 渡す入力
- 指摘の反映経路
- 差し戻し条件

## 起動契約

- adversarial の収束後に1回だけ起動する。起動タイミングと再実行の規約は
  [敵対的レビューループ](adversarial-review.md) の「過剰実装審査との接続」に従う。
- 起動時にプラン入力モードであることを明示し、レビュー済みのプラン文書の全文を渡す。
  明示しない起動は既定の diff 入力モードになるため、プラン審査には使えない。
- 指摘は同じ `PF-*` 台帳へ `reviewer: over-engineering-reviewer` として記録し、指摘IDごとに
  verdict の確定と `adopted` / `rejected` の判断を記録する（敵対的レビューループと同一規約）。
- 指摘の有無にかかわらず審査完了時に `overengineering_snapshot_round` へ adversarial 収束時点の
  `rounds_completed` を記録する。0 findings でも記録して完了とし、未実行の null と区別する。

## 渡す入力

- プラン文書の全文。AC・scope・制約・仮定を本文とは別に列挙して渡さない。列挙すると本文と
  重複した写しが入力の側に残るためである。
- adversarial の収束時点までの指摘台帳。起動はこの1回だけであり、過去の round を追加で実行しない。
- テスト結果は渡さない。プラン時点では存在せず、reviewer も入力として要求しない。

## 指摘の反映経路

- 指摘の反映経路はプラン修正だけとする。採用した指摘は親がプランへ反映する。
- code の変更経路を定義しない。修正対象は code ではなくプランであり、除去経路の判断は
  発生しない。
- 過剰実装審査の指摘を採用してプランを修正した場合は adversarial レビューへ戻らず、修正済みプランで
  レビュー工程を終了する。`over-engineering-reviewer` も再起動しない。不採用だけで修正がなければ審査は完了とする。

## 差し戻し条件

- 「Acceptance Criteria」節に AC が1件も無いプラン文書は、reviewer が判定せず親へ差し戻す。
  親は起草へ戻って AC を確定してから再起動する。節見出しだけあって中身が空の本文が素通りすると、
  必須ゲートである過剰実装審査が実質的に無効になるためである。
- 制約の記載の有無は差し戻し条件にしない。制約が無いプランは本文に「該当なし」と書かれ、
  ユーザーが制約を明示しないプランは正当に存在する。差し戻しは審査完了として記録されないため、
  これを差し戻すと `review-incomplete` が立ち続け、当該プランが `awaiting_review` へ到達できなく
  なる。
- 差し戻しは指摘0件と区別して扱い、`termination` や台帳に審査完了として記録しない。
