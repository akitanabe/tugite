[contracts.test-report-invocation-68edd8617af5]
kind = "requires"
slice = "test-report-invocation"
pattern = "`test-report` の明示指定、または指定範囲のテスト群の理解・把握を求める意図が明確な依頼で開始する。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-no-auto-start-0a4ce17d17dc]
kind = "requires"
slice = "test-report-invocation"
pattern = '''単なる曖昧な相談や、
テストに言及しただけの依頼では開始しない。他 workflow の工程としての自動起動はしない。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-read-only-93a4100756c2]
kind = "requires"
slice = "test-report-read-only"
pattern = '''テスト・コード・成果物の編集、実装、委譲、Worker 起動、後続 Action を行わない。テスト・対象コードの実行も行わない。
観測は静的読解に限る。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-non-evaluation-935b1cf8de40]
kind = "requires"
slice = "test-report-non-evaluation"
pattern = "十分性判定・品質評価をしない。report は観測事実だけを述べ、「不足している」「追加すべき」等の評価・推奨語彙を使わない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-gap-judgment-human-51e2675b0f80]
kind = "requires"
slice = "test-report-non-evaluation"
pattern = "構造的空白の意味判断（埋めるかどうか）は Human に残す。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-scope-inputs-996089b4675f]
kind = "requires"
slice = "test-report-scope"
pattern = "テスト path / directory の明示指定と、機能・領域の自然言語指定の両方を受け付ける。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-code-without-tests-e141c5157fa4]
kind = "requires"
slice = "test-report-scope"
pattern = "テストの存在を前提にしない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-zero-tests-valid-50c80b654ed7]
kind = "requires"
slice = "test-report-scope"
pattern = "対象テストが 0 件であることは、範囲解決不能とは扱わず、有効な観測結果として継続する。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-observed-range-4f175d665227]
kind = "requires"
slice = "test-report-scope"
pattern = "実際に観測した範囲（対象にしたテスト群と対応する対象コード範囲）を、ドメイン語彙の要約とともに report に明記する。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-completeness-split-02204a16c8b4]
kind = "requires"
slice = "test-report-scope"
pattern = "観測の完全性はテスト範囲と対象コード範囲で基準を分ける。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-unresolved-scope-8dde1a96cf19]
kind = "requires"
slice = "test-report-scope"
pattern = "テスト範囲を解決できない場合は、推測で継続せず Human への確認へ返す。解決済み範囲の結果が 0 件である場合はこの経路に入らない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-partial-observation-c9cc59c9ceb7]
kind = "requires"
slice = "test-report-scope"
pattern = '''部分観測は、解決済みテスト file のうち観測していない file が残ることを基準に判定する（解決済み一覧と観測済み一覧の差として
  観測可能であり、agent の自己申告に委ねない）。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-partial-confirm-a86427195fce]
kind = "requires"
slice = "test-report-scope"
pattern = "Human が部分観測での継続を了承した場合のみ継続する。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-omission-conditions-d8558c56b404]
kind = "requires"
slice = "test-report-scope"
pattern = '''対象コード範囲の解決不能は観測可能な2条件（指定から対応する対象コード範囲を一意に特定できない、または対象コードが
  repository 外にある）に限って認め、'''
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-reconstruction-b031e4e54b57]
kind = "requires"
slice = "test-report-reconstruction"
pattern = '''テスト群を振る舞い・シナリオ単位に再構成する。振る舞い名は対象ドメインの語彙で命名し、テスト実装用語（fixture、mock 名等）を
主語にしない。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-execution-state-2527821b4540]
kind = "requires"
slice = "test-report-execution-state"
pattern = "非実行のケースを実行されるケースと同列に「検証されている」として数えない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-no-ci-claim-9b482d2eaf03]
kind = "requires"
slice = "test-report-execution-state"
pattern = "実際の CI job での実行有無など静的に観測できない実行状態は断定しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-gap-relative-c9121a942bab]
kind = "requires"
slice = "test-report-gap-scope"
pattern = "空白の主張は常に指定範囲に相対で述べる（「指定範囲内では観測されない」）。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-no-outside-absence-7877238e9ced]
kind = "requires"
slice = "test-report-gap-scope"
pattern = "範囲外のテストや別手段の検証の不在を断定しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-evidence-eb23350e9807]
kind = "requires"
slice = "test-report-evidence"
pattern = "振る舞い命名、ケース分類、検証手段、空白の全主張に test / code への参照（file 位置等）を付け、"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-config-evidence-1f286681118f]
kind = "requires"
slice = "test-report-evidence"
pattern = "実行状態を runner / test configuration から判断した主張には、その config への参照も付ける。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-overview-order-31bc63acddee]
kind = "requires"
slice = "test-report-overview"
pattern = "全体像先行の階層提示とする: 圧縮した全体像 → 振る舞い別の詳細。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-overview-map-1f156b3c2da5]
kind = "requires"
slice = "test-report-overview"
pattern = "検証体系の見取り図"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-overview-no-ranking-a679d20b01c0]
kind = "requires"
slice = "test-report-overview"
pattern = "重要度による選別・省略はしない"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-overview-nonexec-08fb2c83eb14]
kind = "requires"
slice = "test-report-overview"
pattern = "非実行の有無"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-overview-domain-16c0adf6b7f1]
kind = "requires"
slice = "test-report-overview"
pattern = "観測範囲のドメイン語彙要約"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-overview-static-127541032ff5]
kind = "requires"
slice = "test-report-overview"
pattern = "report が静的観測に基づく旨"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-kernel-heading-4af4bb76df24]
kind = "requires"
slice = "test-report-kernel-loader"
pattern = "## behavior-observation-kernel v1 の parent mapping"
applies_to = ["claude", "codex", "cursor"]
# Why Not: loader fields are one declaration Data block; splitting them could accept a partial mapping.
[contracts.test-report-kernel-loader-data-6eacc7e7426e]
kind = "requires"
slice = "test-report-kernel-loader"
pattern = '''path = ../../references/behavior-observation-kernel.md
load_timing = after scope resolution and any Human confirmation that allows continuation, before report body construction
identity = behavior-observation-kernel-v1
required_sections = [Contract, Method, Reintegration, Consumer の責務, 非目標]
failure = Topology も経路3/4継続本文も出さない。Human 確認へ返す。了承しても Kernel 未検証のまま報告本文を構築しない。4経路へ写さない。stop-incomplete は新設しない
owner = test-report parent
delegate_path_resolution = false'''
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-kernel-load-before-body-6b7b7eade77a]
kind = "requires"
slice = "test-report-kernel-loader"
pattern = "継続を了承したあとの報告本文構築の前に `behavior-observation-kernel-v1` を load し、identity と required sections を検証する。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-kernel-path34-load-cf4ac8598c3a]
kind = "requires"
slice = "test-report-kernel-loader"
pattern = "経路3/4の了承後も Kernel は load する。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-kernel-inject-existing-76427fdaed63]
kind = "requires"
slice = "test-report-kernel-loader"
pattern = "注入先は既存の判定基準 / 必要な周辺 context である。Kernel 専用 channel と返却 field は増やさない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-collective-sufficiency-87696ce8c7b4]
kind = "requires"
slice = "test-report-kernel-loader"
pattern = "Collective Sufficiency は Expected Observation model の導出十分性であり、テスト十分性ではない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-collective-not-center-7666d37f6457]
kind = "requires"
slice = "test-report-kernel-loader"
pattern = "Human 向け中心語にしない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-no-test-grounding-078a0c7b72d9]
kind = "requires"
slice = "test-report-independent-grounding"
pattern = "照合先の対象 test 自身を、その test と照合する Expected Observation の grounding に使わない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-test-only-limits-ad19a50200f8]
kind = "requires"
slice = "test-report-independent-grounding"
pattern = "テストにしか現れ独立 grounding できないものは Observed Test Behavior（`grounding: unresolved`）として Observation Limits へ置く。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-test-only-no-gap-e9d524f02716]
kind = "requires"
slice = "test-report-independent-grounding"
pattern = "その Behavior に Kernel 由来の Expected Observation / Gap を生成しない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-grounding-default-1458968ba05c]
kind = "requires"
slice = "test-report-independent-grounding"
pattern = "独立 grounding は既定で行う。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-no-doc-channel-23bc25ad414c]
kind = "requires"
slice = "test-report-independent-grounding"
pattern = "AC / docs を対象とする独立した突合チャンネル（文書項目対テスト）は作らない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-inrange-spec-context-238fcd458ed2]
kind = "requires"
slice = "test-report-independent-grounding"
pattern = "指定範囲内で authority を持つ specification / contract は、Behavior identity の relevant Context にしてよい。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-no-outside-docs-73062d77f3e0]
kind = "requires"
slice = "test-report-independent-grounding"
pattern = "指定範囲外の AC / docs を探しに行かない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-test-file-completeness-79e65945fc73]
kind = "requires"
slice = "test-report-scope"
pattern = "テスト範囲の完全性は、解決済みテスト file 対 読解済みテスト file の一覧差とする。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-identifying-entry-81690cc7212d]
kind = "requires"
slice = "test-report-scope"
pattern = "その単位を識別する authoritative entry（export / public API / その単位の入口となる既存構造上の面）を実際に読解した単位とする。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-no-relevance-universe-d7b33355a56f]
kind = "requires"
slice = "test-report-scope"
pattern = "relevance の自己申告で宇宙を作らない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-read-not-matching-2074091d6cc4]
kind = "requires"
slice = "test-report-scope"
pattern = "公開面抽出を Gap または突合 Overlay の完了条件にしない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-path3-omit-overlay-6ef6f27dc663]
kind = "requires"
slice = "test-report-scope"
pattern = "Human が了承した場合のみ独立 grounding / Expected Observation 導出 / Gap Overlay を省略し、"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-path3-kernel-loaded-5fec72795297]
kind = "requires"
slice = "test-report-scope"
pattern = "Kernel は load 済みであり、Kernel 未 load とは同一視しない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-path4-limits-only-e954dec4ef30]
kind = "requires"
slice = "test-report-scope"
pattern = "了承時は Observed Test Behavior と Observation Limits のみとし、Expected Observation / Gap は生成しない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-topology-center-d0469c2cc02c]
kind = "requires"
slice = "test-report-reconstruction"
pattern = "report の中心は、独立 grounding した Expected Observation を軸とする Verification Topology である。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-many-to-many-a84707e53be1]
kind = "requires"
slice = "test-report-reconstruction"
pattern = "Expected Observation と Case の many-to-many を意味上保持する。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-relisting-projection-5de76c33d23d]
kind = "requires"
slice = "test-report-reconstruction"
pattern = "同じ Case が複数 Expected Observation に対応する場合の再掲は presentation projection であり、意味上の重複ではない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-case-scenario-7bd73e64c40a]
kind = "requires"
slice = "test-report-reconstruction"
pattern = "Case は意味的シナリオ単位であり、test function 単位ではない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-presentation-label-c40fc78001a3]
kind = "requires"
slice = "test-report-reconstruction"
pattern = "正常系 / 境界値 / 異常系は Topology の構造原理にせず、Human 向け presentation label に降ろす。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-evidence-unit-2944f93b088e]
kind = "requires"
slice = "test-report-verification-means"
pattern = "Evidence は観測主張の最小参照可能単位であり、test function 全体と同一視しない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-boundary-open-727ff8b303d5]
kind = "requires"
slice = "test-report-verification-means"
pattern = "Observation Boundary facts は open-ended な静的事実とする。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-traceable-evidence-611d6d4cf6c4]
kind = "requires"
slice = "test-report-verification-means"
pattern = "Evidence と Observation Boundary facts が Case / Expected Observation から追跡可能である。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-execution-three-3ffaefe9aceb]
kind = "requires"
slice = "test-report-execution-state"
pattern = "execution state は `statically executable` / `statically non-executed` / `indeterminate` とする。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-indeterminate-limits-cb62dfa47c79]
kind = "requires"
slice = "test-report-execution-state"
pattern = "executable がなく、corresponding evidence に `indeterminate` が含まれる場合（non-executed 混在を含む）、Gap にも Unresolved Mapping にもしない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-gap-definition-e2ddd76b6385]
kind = "requires"
slice = "test-report-gap-overlay"
pattern = "Gap は、独立 grounding した Expected Observation に対して、指定範囲（または Human が了承した実観測範囲）内で対応する usable evidence が観測されない状態である。"
applies_to = ["claude", "codex", "cursor"]
# Why Not: judgment order is one procedure; splitting steps could accept a partial Gap Overlay.
[contracts.test-report-gap-judgment-order-4f71306b9b56]
kind = "requires"
slice = "test-report-gap-overlay"
pattern = '''1. 対象 test をその Expected Observation の grounding に使わない。
2. Evidence が独立 grounding 済み Behavior identity に静的帰属でき、Expected Observation の条件が平坦化されずに保持されている場合、correspondence 確定とする。
3. correspondence 確定かつ `statically executable` な corresponding evidence が1つでもある場合、usable evidence observed とする。その Expected Observation は Gap ではない。他に non-executed / indeterminate があっても usable を優先する。
4. executable がなく、corresponding evidence がすべて `statically non-executed` の場合、Gap Overlay の提示差「non-executed evidence only」とする。Unresolved Mapping ではない。
5. executable がなく、corresponding evidence に `indeterminate` が含まれる場合（non-executed 混在を含む）、Gap にも Unresolved Mapping にもしない。Observation Limits（execution state を静的に確定できない）とする。CI 断定しない。
6. corresponding evidence がない場合、Gap Overlay「no corresponding evidence observed」とする。
7. correspondence を静的に確定できない場合、Unresolved Mapping とする。Gap にしない。candidate evidence を添える。'''
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-usable-def-0ce580b85582]
kind = "requires"
slice = "test-report-gap-overlay"
pattern = "`usable` は correspondence 確定かつ `statically executable` である。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-unresolved-not-gap-d254918a429b]
kind = "requires"
slice = "test-report-unresolved-mapping"
pattern = "Unresolved Mapping とする。Gap にしない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-unresolved-not-anon-ad06c9116a73]
kind = "requires"
slice = "test-report-unresolved-mapping"
pattern = "Unresolved Mapping は Observation Limits に匿名化しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-overview-gap-overlay-c29bc41a6a73]
kind = "requires"
slice = "test-report-overview"
pattern = "Gap Overlay の全件"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-overview-unresolved-c5816e94ef20]
kind = "requires"
slice = "test-report-overview"
pattern = "Unresolved Mapping（独立提示。Observation Limits に匿名化しない）"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-overview-obs-limits-1fd44911ae72]
kind = "requires"
slice = "test-report-overview"
pattern = "Observation Limits（Kernel 未確定、scope / partial observation、test-only grounding unresolved、execution state indeterminate 等）"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-no-change-overlay-aa26156a893a]
kind = "requires"
slice = "test-report-change-overlay"
pattern = "Change Overlay は実装しない。予約 field と未使用 schema は作らない。Verification Topology を、追加 overlay を禁止する閉じた schema にしない。"
applies_to = ["claude", "codex", "cursor"]
[contracts.test-report-forbids-public-surface-matching]
kind = "forbids"
pattern = "公開面突合"
paths = [
  "plugins/claude/skills/test-report/SKILL.md",
  "plugins/codex/skills/test-report/SKILL.md",
  "plugins/cursor/skills/test-report/SKILL.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-forbids-internal-gap-kind]
kind = "forbids"
pattern = "テスト群内在の空白"
paths = [
  "plugins/claude/skills/test-report/SKILL.md",
  "plugins/codex/skills/test-report/SKILL.md",
  "plugins/cursor/skills/test-report/SKILL.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-forbids-public-surface-completion]
kind = "forbids"
pattern = "公開面の抽出（公開 API・外部観測可能な振る舞いの列挙）をもって観測とする。"
paths = [
  "plugins/claude/skills/test-report/SKILL.md",
  "plugins/codex/skills/test-report/SKILL.md",
  "plugins/cursor/skills/test-report/SKILL.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-forbids-applicability-unknown-case]
kind = "forbids"
pattern = "該当性を観測できないケース種"
paths = [
  "plugins/claude/skills/test-report/SKILL.md",
  "plugins/codex/skills/test-report/SKILL.md",
  "plugins/cursor/skills/test-report/SKILL.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.test-report-forbids-case-type-axis]
kind = "forbids"
pattern = "正常系 / 境界値 / 異常系（失敗経路）を基本軸として分類する"
paths = [
  "plugins/claude/skills/test-report/SKILL.md",
  "plugins/codex/skills/test-report/SKILL.md",
  "plugins/cursor/skills/test-report/SKILL.md",
]
applies_to = ["claude", "codex", "cursor"]
