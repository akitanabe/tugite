[contracts.deletion-test-method-distribution]
kind = "occurrences"
pattern = "# Deletion Test Method v1"
paths = [
  "plugins/claude/references/deletion-test-method.md",
  "plugins/codex/references/deletion-test-method.md",
  "plugins/cursor/references/deletion-test-method.md",
]
count = 1
applies_to = ["claude", "codex", "cursor"]

[contracts.deletion-test-method-mention-allowlist]
kind = "forbids"
pattern = "deletion-test-method"
exclude_paths = [
  "plugins/claude/references/deletion-test-method.md",
  "plugins/claude/skills/review-refine/SKILL.md",
  "plugins/codex/references/deletion-test-method.md",
  "plugins/codex/skills/review-refine/SKILL.md",
  "plugins/cursor/references/deletion-test-method.md",
  "plugins/cursor/skills/review-refine/SKILL.md",
]
applies_to = ["claude", "codex", "cursor", "claude-marketplace", "codex-marketplace"]

[contracts.dtm-title-requires-0d384abad96a]
kind = "requires"
slice = "deletion-test-method-v1"
pattern = "# Deletion Test Method v1"
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-identity-requires-e5496039ef65]
kind = "requires"
slice = "deletion-test-method-v1"
pattern = "Deletion Test Method identity: `deletion-test-method-v1`."
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-not-kernel-requires-aafaaab146a8]
kind = "requires"
slice = "deletion-test-method-v1"
pattern = "Kernel ではない。Kernel identity と Kernel injection mapping を持たない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-inputs-heading-requires-864bf05c07da]
kind = "requires"
slice = "deletion-test-method-v1"
pattern = "## Inputs"
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-procedure-heading-requires-3cc2da22ad19]
kind = "requires"
slice = "deletion-test-method-v1"
pattern = "## Procedure"
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-results-heading-requires-c704ac568053]
kind = "requires"
slice = "deletion-test-method-v1"
pattern = "## Semantic Results"
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-nongoal-heading-requires-3ed85bd78c22]
kind = "requires"
slice = "deletion-test-method-v1"
pattern = "## Non-goals"
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-inputs-requires-0ab2ce3e4065]
kind = "requires"
slice = "deletion-test-method-v1"
pattern = "stable snapshot"
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-preserves-requires-69a13c01d21c]
kind = "requires"
slice = "deletion-test-method-v1"
pattern = "deletion preserves obligations"
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-breaks-requires-3508e2901d50]
kind = "requires"
slice = "deletion-test-method-v1"
pattern = "deletion breaks obligations"
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-indeterminate-requires-aa00e40d76d8]
kind = "requires"
slice = "deletion-test-method-v1"
pattern = "indeterminate"
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-mutual-witness-requires-49e3d68ca314]
kind = "requires"
slice = "deletion-test-method-v1"
pattern = "同じ selected batch 内の別 candidate を stable remaining witness とみなさない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-mutual-no-preserves-requires-f53064fbea65]
kind = "requires"
slice = "deletion-test-method-v1"
pattern = "mutual witness、evidence 不足、obligation 不明を独立した `preserves` に昇格させず、解消不能なら `indeterminate` とする。"
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-nongoal-admission-requires-7b9cc6c71aaa]
kind = "requires"
slice = "deletion-test-method-v1"
pattern = "scope admission"
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-nongoal-adjudication-requires-c5a8c9bf722b]
kind = "requires"
slice = "deletion-test-method-v1"
pattern = "accept / reject の裁定"
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-forbids-kernel-identity-02244726c126]
kind = "forbids"
slice = "deletion-test-method-v1"
pattern = "Kernel identity:"
applies_to = ["claude", "codex", "cursor"]

[contracts.dtm-forbids-target-membership-proc-6926df9f1625]
kind = "forbids"
slice = "deletion-test-method-v1"
pattern = "Target Membership Check を実行する"
applies_to = ["claude", "codex", "cursor"]
