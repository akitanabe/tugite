[contracts.plan-interactive-no-intermediate-data-field]
kind = "forbids"
pattern = "Intermediate Data:"
applies_to = ["claude", "codex", "cursor"]

# Why Not: projected upstream SKILL.md に Tugite 側の semantic identity / required headings 契約を足さない。他 loader と同様の identity 検証は行わない。
[contracts.clarify-caller-loader-8a889cfe4680]
kind = "requires"
slice = "plan-interactive-clarify-caller"
pattern = '''clarify_it_reference = ../../references/upstream/clarify-it/SKILL.md
clarify_it_load_timing = once at plan-interactive start
clarify_it_snapshot = first successful body frozen for the invocation
clarify_it_failure = existing incomplete; no fallback clarification
clarify_it_identity_check = none'''
applies_to = ["claude", "codex", "cursor"]

[contracts.clarify-caller-data-04b6ae545308]
kind = "requires"
slice = "plan-interactive-clarify-caller"
pattern = '''application = pinned internal upstream clarify-it snapshot in the same plan-interactive parent context
caller_owns = invocation boundary | binding Human authority | verified workflow Data | observation capability | Completed/Stopped workflow projection | direction freeze | downstream workflow
public_extension = none'''
applies_to = ["claude", "codex", "cursor"]

[contracts.clarify-caller-public-app-forbids-83522ec43abc]
kind = "forbids"
slice = "plan-interactive-clarify-caller"
pattern = "application = public clarify-it in the same plan-interactive parent context"
applies_to = ["claude", "codex", "cursor"]

[contracts.clarify-caller-dialogue-norm-forbids-784a79d5b49e]
kind = "forbids"
slice = "plan-interactive-clarify-caller"
pattern = "dialogue_norm = inherit clarify-it Specification, Method, and Casebook without copying or override"
applies_to = ["claude", "codex", "cursor"]

[contracts.clarify-caller-presentation-forbids-7e2eeef8c9bf]
kind = "forbids"
slice = "plan-interactive-clarify-caller"
pattern = "presentation_policy = inherit clarify-it-decision-context presentation policy without copying or override"
applies_to = ["claude", "codex", "cursor"]

[contracts.kernel-loader-data-e30862f1b645]
kind = "requires"
slice = "plan-interactive-kernel-mapping"
pattern = '''resolve_path = ../../references/resolve-kernel.md
resolve_identity = resolve-kernel-v1
resolve_dependencies = none
resolve_required_sections = [Caller boundary と role, Current verified snapshot、working state、frontier, Atomic resolution unit, Exit と停止, Kernel non-dependency]'''
applies_to = ["claude", "codex", "cursor"]

[contracts.kernel-role-data-9e606fc1f1f0]
kind = "requires"
slice = "plan-interactive-kernel-mapping"
pattern = '''caller = plan-interactive
resolver = planner
counterpart = human
authority = binding
ledger = decision ledger'''
applies_to = ["claude", "codex", "cursor"]

[contracts.kernel-loader-no-necessity-path-8302313221e3]
kind = "forbids"
slice = "plan-interactive-kernel-mapping"
pattern = "necessity_path"
applies_to = ["claude", "codex", "cursor"]

[contracts.kernel-loader-no-necessity-identity-0a7f54748a9a]
kind = "forbids"
slice = "plan-interactive-kernel-mapping"
pattern = "necessity_identity"
applies_to = ["claude", "codex", "cursor"]

[contracts.observation-action-permission-cf002f05ca3a]
kind = "forbids"
slice = "plan-interactive-observation-reapply-flow"
pattern = '''contradictory_action_permission = allowed'''
applies_to = ["claude", "codex", "cursor"]

[contracts.projection-reconfirmation-0cb2b27e41e9]
kind = "forbids"
slice = "plan-interactive-direction-freeze-projection"
pattern = '''freeze_only_reconfirmation = required'''
applies_to = ["claude", "codex", "cursor"]

[contracts.advisor-human-boundary-0fcbb37630a6]
kind = "requires"
slice = "plan-interactive-advisor-parent"
pattern = '''automatic_adoption = prohibited
automatic_question_generation = prohibited
direct_human_question = prohibited
human_direction_impact = unresolved -> parent verifies evidence and reconstructs its own recommendation -> clarify-it application
raw_insight = parent-only unless Human explicitly requests process details'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-kernel-before-clarify]
kind = "order"
before = "plan-interactive-kernel-load"
after = "plan-interactive-clarify-start"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-clarify-before-producer]
kind = "order"
before = "plan-interactive-clarify-start"
after = "plan-interactive-constrained-producer-start"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-producer-before-integrity]
kind = "order"
before = "plan-interactive-constrained-producer-start"
after = "plan-interactive-freeze-integrity-start"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-integrity-before-gate]
kind = "order"
before = "plan-interactive-freeze-integrity-start"
after = "plan-interactive-structural-health-gate-start"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-clarify-before-gate]
kind = "order"
before = "plan-interactive-clarify-start"
after = "plan-interactive-structural-health-gate-start"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-gate-before-review]
kind = "order"
before = "plan-interactive-structural-health-gate-start"
after = "plan-interactive-review-refine-handoff"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-quality-advisor-invocations-requires-78e0d7f8cbbc]
kind = "requires"
slice = "plan-quality-advisor-boundary"
pattern = '''verdict = intact | violated | indeterminate
evidence = authority constraint, refined candidate location, semantic delta or indeterminate cause
binding = parent cannot override
comparison = all authority_constraints independently
normal_output = insight_fields only; non-binding; exclude freeze-integrity verdict/evidence
freeze_integrity_output = verdict/evidence only; binding; exclude normal insight'''
applies_to = ["claude", "codex", "cursor"]

[contracts.producer-ownership-c23e6d781a01]
kind = "requires"
slice = "plan-interactive-producer-ownership"
pattern = '''caller = plan-interactive
authority = constrained
authority_constraints = verified freeze 全件
freeze_as_existing_s0 = prohibited
producer_skip = prohibited'''
applies_to = ["claude", "codex", "cursor"]

[contracts.producer-discretionary-forbids-2852a68637c5]
kind = "forbids"
slice = "plan-interactive-producer-ownership"
pattern = "authority = discretionary"
applies_to = ["claude", "codex", "cursor"]

[contracts.producer-subset-forbids-b79030b04694]
kind = "forbids"
slice = "plan-interactive-producer-ownership"
pattern = "authority_constraints = verified freeze の subset"
applies_to = ["claude", "codex", "cursor"]

[contracts.producer-s0-forbids-db8d13b9d3ef]
kind = "forbids"
slice = "plan-interactive-producer-ownership"
pattern = "freeze_as_existing_s0 = allowed"
applies_to = ["claude", "codex", "cursor"]

[contracts.producer-skip-forbids-33c032722ca0]
kind = "forbids"
slice = "plan-interactive-producer-ownership"
pattern = "producer_skip = allowed"
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-clarify-completion-d6edf0d076e7]
kind = "requires"
slice = "plan-interactive-clarify-completion"
pattern = '''- `Completed` → 親が current decision model から freeze に必要な意味単位を抽出する（clarify-it 出力に固定 schema を要求しない）→ `freeze_source_snapshot` → `direction-freeze-projection`
- `Stopped` → 停止理由と未解決 Human decisions / inconsistency を保持した既存 `incomplete`。独自 clarification 継続も、独自 recovery / continuation status も作らない'''
applies_to = ["claude", "codex", "cursor"]

[contracts.completed-not-plan-99282512b220]
kind = "requires"
slice = "plan-interactive-clarify-completion"
pattern = "`clarify-it: Completed` は Human Decision Context の completion であり、Plan Artifact completion ではない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.completed-is-plan-forbids-abe5e34a1449]
kind = "forbids"
slice = "plan-interactive-clarify-completion"
pattern = "`clarify-it: Completed` は Plan Artifact completion である"
applies_to = ["claude", "codex", "cursor"]

[contracts.integrity-fresh-4adbf99266e3]
kind = "requires"
slice = "plan-interactive-freeze-integrity"
pattern = "stale check や subset check を再利用しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.integrity-triggers-357745a180db]
kind = "requires"
slice = "plan-interactive-freeze-integrity"
pattern = '''- 各 producer invocation が `complete` で返した latest S2 の後。初回、gate retry、new freeze 後の recovery を含む。
- `review-refine` の adopted revision を反映した latest snapshot の後。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.integrity-detail-ok-8ed29e75647b]
kind = "requires"
slice = "plan-interactive-freeze-integrity"
pattern = "全 frozen meaning を保持した AC / verification / local detail の追加は許可し、baseline 全文を変更禁止対象にしない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.gate-intact-only-bb321f55fbd7]
kind = "requires"
slice = "plan-interactive-structural-health-gate"
pattern = "producer `complete` かつ freeze-integrity `intact` の current verified candidate だけを"
applies_to = ["claude", "codex", "cursor"]

[contracts.gate-no-direct-freeze-8c6a0140186b]
kind = "requires"
slice = "plan-interactive-structural-health-gate"
pattern = "direction freeze や `clarify-it: Completed` を gate へ直接送らない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.gate-freeze-candidate-forbids-ec06e8df3a6d]
kind = "forbids"
slice = "plan-interactive-structural-health-gate"
pattern = "direction freeze 候補を受け取った場合"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-review-goal-requires-e317cdac5f5f]
kind = "requires"
slice = "plan-interactive-handoff"
pattern = "`review_goal` は、ユーザー指定の review goal や追加の具体的な risk がない場合、「実装前プランの具体的な failure path を確認し、確定候補にできるか判断する」とする。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-review-goal-requires-84c3a0db656b]
kind = "requires"
slice = "plan-interactive-handoff"
pattern = "これは plan review 自体の既定目的であり、毎回 risk を事前発見することを要求しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-review-goal-requires-5931402b4335]
kind = "requires"
slice = "plan-interactive-handoff"
pattern = "ユーザー指定 goal や追加 risk は既存 reviewer の責務内で追加できる。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-review-goal-forbids-cc2af17c3ef2]
kind = "forbids"
slice = "plan-interactive-handoff"
pattern = "追加 risk がなければ `review_goal` を設定しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-fallback-forbids-5e98273e0804]
kind = "forbids"
slice = "plan-interactive-handoff"
pattern = "ただし goal に対応する別 reviewer があれば `review-refine` へ渡す"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-freeze-as-final-forbids-f925600a9eee]
kind = "forbids"
slice = "plan-interactive-final-acceptance-routing"
pattern = "direction freeze を final acceptance とみなす"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-optional-final-forbids-029c6c34cc5f]
kind = "forbids"
slice = "plan-interactive-final-acceptance-routing"
pattern = "final acceptance は任意"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-freeze-as-final-variant-forbids-f49f11fc078b]
kind = "forbids"
slice = "plan-interactive-final-acceptance-routing"
pattern = "direction freeze を最終承認として扱う"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-optional-final-variant-forbids-e58b5d722649]
kind = "forbids"
slice = "plan-interactive-final-acceptance-routing"
pattern = "final acceptance は省略可能"
applies_to = ["claude", "codex", "cursor"]

# Why Not: consumer acceptance timing, artifact purity, and projection stay with plan-interactive while publication is delegated to the shared reference.
[contracts.plan-interactive-artifact-completion-d9413ffc075f]
kind = "requires"
slice = "plan-interactive-artifact-completion"
pattern = '''workflow = plan-interactive
artifact_eligibility = final acceptance completed or explicit opt-out and verified publication_target
pre_acceptance_artifact = none at direction freeze, gate, review, or acceptance candidate
artifact_body = frozen final accepted candidate body only
artifact_excludes = [Semantic Delta, Verification Delta, Human Attention, gate result, review result, decision ledger, finding ledger, process history]
stdout = Result, short Summary, optional Human Attention, Artifact local path'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-publication-loader-970692b302a3]
kind = "requires"
slice = "plan-interactive-local-artifact-completion"
pattern = '''publication_reference = ../../references/plan-artifact-publication.md
publication_load_timing = once before programmatic-publication use
publication_identity = plan-artifact-publication-v1
publication_use = parent-confirmed publication_target -> programmatic-publication -> outward status/stdout projection'''
applies_to = ["claude", "codex", "cursor"]

# Why Not: dual load, Git predicate scope, and consumer 3-value must stay coherent; isolated loader presence could hide missing selection mapping.
[contracts.plan-interactive-destination-selection-c998bc5a2189]
kind = "requires"
slice = "plan-interactive-destination-selection"
pattern = '''destination_reference = ../../references/destination-selection.md
destination_load_timing = once before Agentic destination confirmation
destination_identity = destination-selection-v1
destination_required_sections = [Inputs / Outputs, Candidate facts, caller_owned_predicates の適用範囲, Programmatic Flows, Agentic unique selection, destination-reselection]
destination_failure = existing incomplete path; no new status'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-destination-selection-d53011ff8183]
kind = "requires"
slice = "plan-interactive-destination-selection"
pattern = "Git ignored/index predicates を project-local および canonical path が repository 内に入る OS-temp にだけ適用する範囲で渡す。repository 外の verified OS-temp に Git predicates を適用しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-destination-selection-9c3aa17ffab7]
kind = "requires"
slice = "plan-interactive-destination-selection"
pattern = '''1. explicit 確定 → それを使う
2. qualified set のときだけ unique selection 本文を適用する
3. explicit incomplete および入力不足 incomplete → unique selection を起動せず incomplete。`publication_target` を組まない'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-destination-selection-e2e7ffc5f723]
kind = "requires"
slice = "plan-interactive-destination-selection"
pattern = "`destination-reselection-required` を受けたとき、元が Human explicit なら同じ requested_destination を保持し unique-best auto-select へ落とさない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-destination-selection-1e7202613c42]
kind = "requires"
slice = "plan-interactive-destination-selection"
pattern = "decoy 一覧 HOW と ranking procedure をこの Skill に置かない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-no-retired-autosave]
kind = "forbids"
pattern = "workflow は成果物を自律的に保存しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-flow-section-occurrences]
kind = "occurrences"
pattern = "## Programmatic Flows"
count = 1
paths = [
  "plugins/claude/skills/plan-interactive/SKILL.md",
  "plugins/codex/skills/plan-interactive/SKILL.md",
  "plugins/cursor/skills/plan-interactive/SKILL.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-local-artifact-flow-occurrences]
kind = "occurrences"
pattern = "### local-artifact-completion"
count = 1
paths = [
  "plugins/claude/skills/plan-interactive/SKILL.md",
  "plugins/codex/skills/plan-interactive/SKILL.md",
  "plugins/cursor/skills/plan-interactive/SKILL.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-local-artifact-completion-8c0d65863538]
kind = "requires"
slice = "plan-interactive-local-artifact-completion"
pattern = "以下は final acceptance または明示 opt-out の後に、親が exact target を確定して渡した publication routing だけを持つ。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-local-artifact-flow-564c89523b45]
kind = "requires"
slice = "plan-interactive-local-artifact-completion"
pattern = "Trigger: final acceptance が完了した、または Human が final acceptance を明示 opt-out し、親が exact `publication_target` を確定したとき。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-local-artifact-flow-f80c5da63d79]
kind = "requires"
slice = "plan-interactive-local-artifact-completion"
pattern = "Inputs: final acceptance / opt-out Data、凍結した成果物本文 bytes、および親確定の `publication_target` Data。`publication_target` は existing destination の observed destination object identity、または OS-temp の verified temp-root identity / top-level `exact_destination` / exclusive creation intent の排他的 Data を持ち、作成前の directory object identity を要求しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-local-artifact-flow-no-future-23b1699fa299]
kind = "forbids"
slice = "plan-interactive-local-artifact-completion"
pattern = "Inputs: final acceptance / opt-out Data、凍結した成果物本文 bytes、exact destination、exact filename、finite retry bound、qualification evidence、destination object identity、および必要な OS-temp identity / creation intent。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-local-artifact-flow-7d9a58cb9310]
kind = "requires"
slice = "plan-interactive-local-artifact-completion"
pattern = "Outcomes: published result と `final-candidate` の outward status / stdout projection、`destination-reselection-required`、または `incomplete`。"
applies_to = ["claude", "codex", "cursor"]

# Why Not: file-global field counts can be satisfied by a decoy after the real Flow loses its Procedure.
[contracts.plan-interactive-local-artifact-flow-661be7a32758]
kind = "requires"
slice = "plan-interactive-local-artifact-completion"
pattern = '''Trigger: final acceptance が完了した、または Human が final acceptance を明示 opt-out し、親が exact `publication_target` を確定したとき。
Inputs: final acceptance / opt-out Data、凍結した成果物本文 bytes、および親確定の `publication_target` Data。`publication_target` は existing destination の observed destination object identity、または OS-temp の verified temp-root identity / top-level `exact_destination` / exclusive creation intent の排他的 Data を持ち、作成前の directory object identity を要求しない。
Procedure: skill-relative `../../references/plan-artifact-publication.md` を publication invocation 前に一度だけ load し、identity `plan-artifact-publication-v1` と必要本文を検証する。検証済み reference の `programmatic-publication` Flow に、親確定の `publication_target` をそのまま渡す。Flow の published result から outward status と stdout の Result、Summary、必要な Human Attention、Artifact path だけを projection する。consumer は target selection、candidate ranking、filename、retry bound、publication procedure を再実行・複製しない。
```text
publication_reference = ../../references/plan-artifact-publication.md
publication_load_timing = once before programmatic-publication use
publication_identity = plan-artifact-publication-v1
publication_use = parent-confirmed publication_target -> programmatic-publication -> outward status/stdout projection
```
Outcomes: published result と `final-candidate` の outward status / stdout projection、`destination-reselection-required`、または `incomplete`。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-local-artifact-flow-order]
kind = "order"
before = "plan-interactive-final-acceptance-complete"
after = "plan-interactive-publication-inputs"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-local-artifact-flow-loader-before-outcomes]
kind = "order"
before = "plan-interactive-publication-loader"
after = "plan-interactive-publication-outcomes"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-programmatic-flow-cf0d1bbeda96]
kind = "requires"
slice = "plan-interactive-programmatic-flows"
pattern = "以下は、親が意味判断を完了して確定 Data を渡した後の局所的な deterministic routing だけを持つ。Flow の procedure、条件、outcome は固定であり、Agent は override、bypass、置換しない。outcome 後に複数の妥当な Action が残る意味判断は親へ返す。"
applies_to = ["claude", "codex", "cursor"]

[contracts.advisor-parent-data-4abebd6b2b0c]
kind = "requires"
slice = "plan-interactive-advisor-parent"
pattern = '''trigger_only = scope or responsibility boundary change | adopted-Claim dependency | decision-completing additional Claim | non-trivial pre-freeze change chain
advisor = read-only plan-quality-advisor
input = candidate snapshot + verified criteria Data
insight = non-binding Data
adjudicator = plan-interactive parent against primary evidence and request
mapping = adopted | rejected | unresolved in adoption ledger
ledger_boundary = decision ledger is separate from adoption ledger
fixed_phase = prohibited
empty_insights = not direction freeze evidence'''
applies_to = ["claude", "codex", "cursor"]

[contracts.advisor-auto-adopt-ee71d5d4c850]
kind = "forbids"
slice = "plan-interactive-advisor-parent"
pattern = "insight を自動採用する"
applies_to = ["claude", "codex", "cursor"]

[contracts.advisor-auto-question-82ec8ab8d9f7]
kind = "forbids"
slice = "plan-interactive-advisor-parent"
pattern = "insight から質問を自動生成する"
applies_to = ["claude", "codex", "cursor"]

[contracts.advisor-fixed-phase-017074626a22]
kind = "forbids"
slice = "plan-interactive-advisor-parent"
pattern = "advisor を固定 phase とする"
applies_to = ["claude", "codex", "cursor"]

[contracts.advisor-empty-freeze-e3788c71478c]
kind = "forbids"
slice = "plan-interactive-advisor-parent"
pattern = "`insights: []` を direction freeze の根拠にする"
applies_to = ["claude", "codex", "cursor"]

[contracts.verification-parent-data-cd64ef2a5484]
kind = "requires"
slice = "plan-interactive-verification-derivation"
pattern = '''timing = before direction freeze and after each updated snapshot
owner = plan-interactive parent Calculation
input = Task Specification + verified workflow Data
coverage = normal | boundary | failure path | side effect | prohibition | responsibility boundary | scope exclude
output = how each obligation will be observed after implementation
clarify_it = do not add this derivation or its schema to clarify-it
blocking_gap = incomplete before direction freeze'''
applies_to = ["claude", "codex", "cursor"]

# Why Not: each Flow keeps one deterministic routing witness while Human meaning and acceptance remain parent-owned.

[contracts.pi-kernel-preflight-81716b5f1dd4]
kind = "requires"
slice = "plan-interactive-interactive-kernel-preflight"
pattern = '''Trigger: 親が最初の clarify-it を開始する直前で、kernel preflight を要求したとき。
Inputs: resolve-kernel の skill-relative path、identity、必要 section、dependencies、plan-interactive の caller/role mapping、親の判定基準。
Procedure: resolve は clarify-it 適用前に一度だけ load・identity・必要本文を検証する。resolve の failure は clarify-it 前に `incomplete` とし、検証済み本文だけを親の既存判定基準へ注入する。path 解決と最終判断は親が所有する。
Outcomes: 検証済み kernel Data と role mapping、または `incomplete`。Flow は Human の意味判断、clarify-it の対話結果を expected oracle にせず、loader failure を突破しない。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-observation-reapply-flow-01c538047cd1]
kind = "requires"
slice = "plan-interactive-observation-reapply-flow"
pattern = '''Trigger: 親の最小観測後も同じ decision model に追加の技術 evidence が必要、または既存 evidence と runtime behavior の不一致が観測されたとき。
Inputs: initial Action 実行前 Data として、同じ current decision model、親確定の最小 observation request / criteria、required runtime behavior、同じ clarify-it parent context、current verified context。observation result と action evidence は含めない。
Procedure: 親確定の最小 Observation Action を一度だけ実行し、result と provenance / evidence を中間 Data として freeze して Agentic 親へ返す。technical evidence gap は親境界へ返し、Human Decision Point や clarify-it Stopped に変換しない。contradictory Action、新しい status、固定 output schema、別 context は導入しない。decision-model への再適用は Flow に置かない。
Outcomes: observation result と provenance / evidence、または親へ返す technical evidence gap。Flow は observation の意味評価、decision-model への再適用、Human の clarification 内容を expected oracle にしない。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-direction-freeze-d3f845d99a5c]
kind = "requires"
slice = "plan-interactive-direction-freeze-projection"
pattern = '''Trigger: clarify-it `Completed` の verified Data comparison が一意の direction freeze candidate projection を許可したとき。
Inputs: initial Calculation 実行前 Data として、要求原文、verified decision ledger、全意味単位を含む raw `freeze_source_snapshot`、source evidence、workflow-local opaque ID generation input。direction freeze candidate は含めない。
Procedure: source item ごとに非空で一意な ID を一度だけ付与し、source から projection した direction freeze candidate と exact comparison result を中間 Data として扱う。source ID 集合と constraint ID 集合の exact bijection、未投影・重複・余分・meaning mismatch の不在を全数照合し、照合済み `{id, frozen_meaning, source_evidence}` だけを candidate Outcome へ projection する。新しい meaning、condition、decision は加えず、不完全・空・再生成・曖昧な投影は producer 前に `stop-incomplete` とする。
Outcomes: verified Data comparison に対応した unique direction freeze candidate projection、または producer を禁止する `stop-incomplete`。Human Decision、clarification 内容、evidence の意味評価、candidate の採否は親が保持し expected oracle にしない。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-producer-routing-c406feba3d3a]
kind = "requires"
slice = "plan-interactive-constrained-producer-routing"
pattern = '''Trigger: 親が verified direction freeze 全件を immutable `authority_constraints` として確定し、mandatory constrained producer の routing を要求したとき。
Inputs: initial Action 実行前 Data として、要求原文、repository observation、`authority = constrained`、verified freeze 全件の `authority_constraints`、producer invocation Data。direction freeze を existing verified candidate / 初期 S0 としては含めない。producer result は含めない。
Procedure: producer を必ず一度起動する。direction freeze から gate / review / final acceptance へ直行せず、additional refinement の Human choice で base synthesis を skip しない。producer `stop-incomplete` は downstream を起動せず `incomplete` とする。`authority_conflict` は constraint ID、evidence、影響範囲とともに Human boundary へ返す。`complete` の latest S2 だけを freeze-integrity へ送る。
Outcomes: producer-complete latest S2 の integrity routing、`authority_conflict` の Human boundary、または `incomplete`。constraint の意味変更、candidate の採否、Human Decision は親裁定であり Flow は expected oracle にしない。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.producer-skip-gate-forbids-1d21168212f6]
kind = "forbids"
slice = "plan-interactive-constrained-producer-routing"
pattern = "direction freeze から structural-health-gate へ直行する"
applies_to = ["claude", "codex", "cursor"]

[contracts.producer-optional-skip-forbids-ae9ec6682749]
kind = "forbids"
slice = "plan-interactive-constrained-producer-routing"
pattern = "「この方向で確定し、proposal refinement は行わない」は producer と freeze-integrity を実行せず structural-health-gate へ送る"
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-integrity-routing-bdc7f7d9a5f4]
kind = "requires"
slice = "plan-interactive-freeze-integrity-routing"
pattern = '''Trigger: producer が `complete` で返した latest S2 の後、または `review-refine` の adopted revision を反映した latest snapshot の後に、親が fresh freeze-integrity を要求したとき。
Inputs: initial Action 実行前 Data として、全 constraint ID / `frozen_meaning` / `source_evidence`、latest candidate、trigger 種別（post-producer / post-review）、fixed bounds / invocation Data。stale check evidence と subset check は含めない。Human decision と recovery freeze は含めない。
Procedure: 全 constraints を latest candidate に対して fresh に独立照合する。stale check や subset check を再利用しない。`intact` は次の gate、review dispatch、または final acceptance へ進める。post-producer `violated` は constraint を変更せずには安全な candidate を作れない場合、`authority_conflict` として Human boundary へ返し、new freeze 後は producer から再開する。post-review `violated` は violating working snapshot を promote せず、last constraint-intact verified snapshot を維持し、影響 finding を `human-confirmation` へ再裁定する。Human が freeze を維持する場合は finding を再裁定し、安全な candidate に対する changed-scope review を経て final acceptance へ進む。Human が direction を変える場合は `Human decision → verified new freeze → constrained producer complete latest S2 → fresh integrity → gate → review dispatch → final acceptance` の順で再入する。`indeterminate` は Agent authority 内で evidence を取得できる場合だけ同じ candidate を fresh に再照合する。取得不能、closure 不成立、または fixed budget 到達では `incomplete` とする。violating snapshot を再開 baseline にしない。
Outcomes: `intact` の downstream routing、post-producer `authority_conflict`、post-review last-intact 維持と `human-confirmation`、または `incomplete`。Human recovery の内容、evidence の意味評価、finding の採否は親が裁定し expected oracle にしない。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.integrity-stale-forbids-4ddc93e6e458]
kind = "forbids"
slice = "plan-interactive-freeze-integrity-routing"
pattern = "stale check を再利用する"
applies_to = ["claude", "codex", "cursor"]

[contracts.integrity-subset-forbids-51f195bad750]
kind = "forbids"
slice = "plan-interactive-freeze-integrity-routing"
pattern = "subset check を再利用する"
applies_to = ["claude", "codex", "cursor"]

[contracts.integrity-nonintact-final-forbids-ba956188ba30]
kind = "forbids"
slice = "plan-interactive-freeze-integrity-routing"
pattern = "non-intact から final acceptance へ直行する"
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-gate-reopen-6803d4f7eb5c]
kind = "requires"
slice = "plan-interactive-structural-gate-reopen-routing"
pattern = '''Trigger: structural-health-gate が current candidate に対する `return`、`pass`、または `insufficient-evidence` を親へ返したとき。
Inputs: initial Action 実行前 Data として、gate result / evidence の problem / impact / recommendation、独立した `rounds.limit`、current round、fixed parent context、current verified candidate、同じ immutable `authority_constraints`。Human response、affected closure、new freeze、producer result、integrity result は含めない。
Procedure: `pass` は review dispatch へ送る。`return` かつ current round が limit 未満の場合だけ finding を Agentic 親へ返す。親が constraints 内の具体化で閉じると裁定した場合は、current verified candidate と同じ constraints で producer を retry し、`complete` 後に fresh integrity check を行う。constraint 変更が必要なら local `clarify-it → new freeze → constrained producer` へ戻し、complete 後に fresh integrity を行う。各 result / evidence は中間 Data として親へ戻し、親裁定 Data を再投入してから次へ進む。前 cycle の freeze / constraints / review opt-out を次 cycle の既定として継承しない。limit 到達、`insufficient-evidence`、closure failure、または evidence 不足は `incomplete` とし、別 identity を作らない。old integrity evidence を再利用しない。
Outcomes: review dispatch、budget 内 producer retry または new-freeze recovery、または `incomplete`。gate finding の意味、Human response、constraint 変更要否、candidate の採否は親入力・親裁定であり Flow は expected oracle を固定しない。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.gate-old-integrity-forbids-aa125686e9bf]
kind = "forbids"
slice = "plan-interactive-structural-gate-reopen-routing"
pattern = "old integrity evidence を再利用する"
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-review-dispatch-355a7d990198]
kind = "requires"
slice = "plan-interactive-review-dispatch"
pattern = '''Trigger: 親が artifact snapshot と review readiness を確定し、review routing を要求したとき。
Inputs: 親確定の artifact_kind、applicability、user review opt-out、review goal、reviewer data、Acceptance Criteria / 設計 readiness、reviewer availability。
Procedure: parent-confirmed applicability は opt-out より先に評価する。nonapplicable は normal completion、applicable + opt-out は normal completion、applicable + no opt-out + ready は review dispatch とする。readiness failure または reviewer failure は review-not-established として親へ返す。
Outcomes: nonapplicable、opt-out、review dispatch、または明示的な `review-not-established`。Flow は accept を返さず、finding の意味的な採否と artifact status は親へ返す。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.review-skip-incomplete-forbids-20df0d368789]
kind = "forbids"
slice = "plan-interactive-review-dispatch"
pattern = "applicable かつ明示 skip は review 未実施の draft と残存 risk を `incomplete` として返す"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-optout-first-forbids-13dce9bf9286]
kind = "forbids"
slice = "plan-interactive-review-dispatch"
pattern = "parent-confirmed opt-out は applicability より先に評価する"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-readiness-normal-forbids-ddd5ee4db11c]
kind = "forbids"
slice = "plan-interactive-review-dispatch"
pattern = "readiness failure または reviewer failure は normal completion とする"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-termination-status-forbids-0ba76e386e81]
kind = "forbids"
slice = "plan-interactive-review-dispatch"
pattern = "termination を artifact status として返す"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-no-caller-status-forbids-afe45cf08018]
kind = "forbids"
slice = "plan-interactive-review-dispatch"
pattern = "artifact status は review-refine が返す"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-decoy-forbids-7568b897d34d]
kind = "forbids"
slice = "plan-interactive-review-dispatch"
pattern = "applicability は opt-out より後でもよい"
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-final-acceptance-87570bbad709]
kind = "requires"
slice = "plan-interactive-final-acceptance-routing"
pattern = '''Trigger: gate と review dispatch を通過した candidate、または review 非適用 / explicit opt-out の draft が final acceptance の親判定へ到達したとき。
Inputs: initial Action 実行前 Data として、final candidate、最新 direction freeze を Semantic Delta baseline とする Data、summary、direction change、verification result、remaining risk、default required、pre-existing binding explicit opt-out、acceptance invocation Data。Human Decision と correction classification result は含めない。
Procedure: direction freeze、gate pass、review 済み candidate を final acceptance と同一視せず、既定で acceptance Action と report を要求する。explicit opt-out は approval Action だけを省略し report を残す。それ以外は acceptance Action を一度実行し、Human response を中間 Data として Agentic 親へ返す。親が裁定した local / large / closure classification の再投入後だけ routing する。local correction は affected closure の local `clarify-it → verified new freeze → constrained producer complete` を順に routing し、complete 後の fresh integrity、gate、changed-scope review、再 acceptance を経る。各 autonomous result と親裁定 Data を中間 Data として parent round-trip する。large purpose / scope change は public workflow 全体を再策定し、closure failure は `incomplete` とする。non-intact な snapshot から final acceptance へ直行しない。
Outcomes: final acceptance、明示 opt-out 後の report、local correction loop、large reformulation、または closure failure の `incomplete`。local / large / closure の分類、Human acceptance、verification の意味評価、最終採否は親入力・親裁定であり Flow は expected oracle にしない。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.final-nonintact-forbids-bc46e28bb360]
kind = "forbids"
slice = "plan-interactive-final-acceptance-routing"
pattern = "non-intact な snapshot から final acceptance へ直行する"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-programmatic-flow-trigger-occurrences]
kind = "occurrences"
pattern = '''Trigger:'''
count = 9
paths = ["plugins/claude/skills/plan-interactive/SKILL.md", "plugins/codex/skills/plan-interactive/SKILL.md", "plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-programmatic-flow-inputs-occurrences]
kind = "occurrences"
pattern = '''Inputs:'''
count = 9
paths = ["plugins/claude/skills/plan-interactive/SKILL.md", "plugins/codex/skills/plan-interactive/SKILL.md", "plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-programmatic-flow-procedure-occurrences]
kind = "occurrences"
pattern = '''Procedure:'''
count = 9
paths = ["plugins/claude/skills/plan-interactive/SKILL.md", "plugins/codex/skills/plan-interactive/SKILL.md", "plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-programmatic-flow-outcomes-occurrences]
kind = "occurrences"
pattern = '''Outcomes:'''
count = 9
paths = ["plugins/claude/skills/plan-interactive/SKILL.md", "plugins/codex/skills/plan-interactive/SKILL.md", "plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-kernel-preflight-occurrences]
kind = "occurrences"
pattern = "### interactive-kernel-preflight"
count = 1
paths = ["plugins/claude/skills/plan-interactive/SKILL.md", "plugins/codex/skills/plan-interactive/SKILL.md", "plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-observation-reapply-flow-occurrences]
kind = "occurrences"
pattern = "### observation-reapply"
count = 1
paths = ["plugins/claude/skills/plan-interactive/SKILL.md", "plugins/codex/skills/plan-interactive/SKILL.md", "plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-direction-freeze-occurrences]
kind = "occurrences"
pattern = "### direction-freeze-projection"
count = 1
paths = ["plugins/claude/skills/plan-interactive/SKILL.md", "plugins/codex/skills/plan-interactive/SKILL.md", "plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-producer-routing-occurrences]
kind = "occurrences"
pattern = "### constrained-producer-routing"
count = 1
paths = ["plugins/claude/skills/plan-interactive/SKILL.md", "plugins/codex/skills/plan-interactive/SKILL.md", "plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-integrity-routing-occurrences]
kind = "occurrences"
pattern = "### freeze-integrity-routing"
count = 1
paths = ["plugins/claude/skills/plan-interactive/SKILL.md", "plugins/codex/skills/plan-interactive/SKILL.md", "plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-gate-reopen-occurrences]
kind = "occurrences"
pattern = "### structural-gate-reopen-routing"
count = 1
paths = ["plugins/claude/skills/plan-interactive/SKILL.md", "plugins/codex/skills/plan-interactive/SKILL.md", "plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-review-dispatch-occurrences]
kind = "occurrences"
pattern = "### review-dispatch"
count = 1
paths = ["plugins/claude/skills/plan-interactive/SKILL.md", "plugins/codex/skills/plan-interactive/SKILL.md", "plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-final-acceptance-occurrences]
kind = "occurrences"
pattern = "### final-acceptance-routing"
count = 1
paths = ["plugins/claude/skills/plan-interactive/SKILL.md", "plugins/codex/skills/plan-interactive/SKILL.md", "plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-retired-optional-refinement-occurrences]
kind = "occurrences"
pattern = "### optional-proposal-refinement-routing"
count = 0
paths = ["plugins/claude/skills/plan-interactive/SKILL.md", "plugins/codex/skills/plan-interactive/SKILL.md", "plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-retired-integrity-recovery-occurrences]
kind = "occurrences"
pattern = "### freeze-integrity-recovery-routing"
count = 0
paths = ["plugins/claude/skills/plan-interactive/SKILL.md", "plugins/codex/skills/plan-interactive/SKILL.md", "plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.pi-retired-fixed-review-occurrences]
kind = "occurrences"
pattern = "### fixed-review-routing"
count = 0
paths = ["plugins/claude/skills/plan-interactive/SKILL.md", "plugins/codex/skills/plan-interactive/SKILL.md", "plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]
