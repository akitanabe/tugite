[contracts.plan-interactive-document]
kind = "occurrences"
pattern = "# plan-interactive"
count = 1
paths = [
  "plugins/claude/skills/plan-interactive/SKILL.md",
  "plugins/codex/skills/plan-interactive/SKILL.md",
  "plugins/cursor/skills/plan-interactive/SKILL.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-ownership-e59db0d27680]
kind = "requires"
slice = "plan-interactive-ownership"
pattern = "exactly one の task-local Local Model を所有"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-authority-4fcffc0dc724]
kind = "requires"
slice = "plan-interactive-authority"
pattern = "downstream authority obligations として確立します。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-core-input-f09a90a98fb9]
kind = "requires"
slice = "plan-interactive-core-input"
pattern = "Human-confirmed direction、authority constraints、resolved upstream evidence を共通の Planning Core へ渡します。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-authority-preservation-3efbe97c71e7]
kind = "requires"
slice = "plan-interactive-authority-preservation"
pattern = "direction や constraints を変更、拡張、緩和、置換せず"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-incomplete-627dfd75fd1a]
kind = "requires"
slice = "plan-interactive-incomplete"
pattern = "direction を変えず `incomplete` を返し"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-human-boundary-270d3e1ef25a]
kind = "requires"
slice = "plan-interactive-human-boundary"
pattern = "第二の mandatory Human acceptance ではありません。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-result-09c971fed184]
kind = "requires"
slice = "plan-interactive-result"
pattern = "後処理で candidate を変更しません。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-plan-only-99c5358fe59a]
kind = "requires"
slice = "plan-interactive-plan-only"
pattern = "implementation、delegation、Issue / PR 更新、downstream workflow を開始しません。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-document-before-ownership]
kind = "order"
before = "plan-interactive-document-relation"
after = "plan-interactive-ownership-relation"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-ownership-before-authority]
kind = "order"
before = "plan-interactive-ownership-relation"
after = "plan-interactive-authority-relation"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-authority-before-core]
kind = "order"
before = "plan-interactive-authority-relation"
after = "plan-interactive-core-input-relation"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-core-before-preservation]
kind = "order"
before = "plan-interactive-core-input-relation"
after = "plan-interactive-authority-preservation-relation"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-preservation-before-incomplete]
kind = "order"
before = "plan-interactive-authority-preservation-relation"
after = "plan-interactive-incomplete-relation"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-incomplete-before-human]
kind = "order"
before = "plan-interactive-incomplete-relation"
after = "plan-interactive-human-boundary-relation"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-human-before-result]
kind = "order"
before = "plan-interactive-human-boundary-relation"
after = "plan-interactive-result-relation"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-result-before-completion]
kind = "order"
before = "plan-interactive-result-relation"
after = "plan-interactive-plan-only-relation"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-interactive-claude-invocation-policy]
kind = "occurrences"
pattern = "disable-model-invocation: true"
count = 1
paths = ["plugins/claude/skills/plan-interactive/SKILL.md"]
applies_to = ["claude"]

[contracts.plan-interactive-codex-invocation-policy]
kind = "occurrences"
pattern = "allow_implicit_invocation: false"
count = 1
paths = ["plugins/codex/skills/plan-interactive/agents/openai.yaml"]
applies_to = ["codex"]

[contracts.plan-interactive-cursor-invocation-policy]
kind = "occurrences"
pattern = "disable-model-invocation: true"
count = 1
paths = ["plugins/cursor/skills/plan-interactive/SKILL.md"]
applies_to = ["cursor"]
