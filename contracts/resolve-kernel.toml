[contracts.resolve-kernel-distribution]
kind = "occurrences"
pattern = "# Resolve Kernel v1"
paths = [
  "plugins/claude/references/resolve-kernel.md",
  "plugins/codex/references/resolve-kernel.md",
  "plugins/cursor/references/resolve-kernel.md",
]
count = 1
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-reference-identity]
kind = "occurrences"
pattern = "Kernel identity: `resolve-kernel-v1`."
paths = [
  "plugins/claude/references/resolve-kernel.md",
  "plugins/codex/references/resolve-kernel.md",
  "plugins/cursor/references/resolve-kernel.md",
]
count = 1
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-mention-allowlist]
kind = "forbids"
pattern = "resolve-kernel"
exclude_paths = [
  "plugins/claude/references/resolve-kernel.md",
  "plugins/claude/skills/plan-agent/SKILL.md",
  "plugins/claude/skills/plan-interactive/references/clarification.md",
  "plugins/claude/skills/plan-candidate-producer/SKILL.md",
  "plugins/codex/references/resolve-kernel.md",
  "plugins/codex/skills/plan-agent/SKILL.md",
  "plugins/codex/skills/plan-interactive/references/clarification.md",
  "plugins/codex/skills/plan-candidate-producer/SKILL.md",
  "plugins/cursor/references/resolve-kernel.md",
  "plugins/cursor/skills/plan-agent/SKILL.md",
  "plugins/cursor/skills/plan-interactive/references/clarification.md",
  "plugins/cursor/skills/plan-candidate-producer/SKILL.md",
]
applies_to = ["claude", "codex", "cursor", "claude-marketplace", "codex-marketplace"]

[contracts.resolve-kernel-mapping-forbids-30e9e5d5c1df]
kind = "forbids"
slice = "resolve-kernel-v1"
pattern = "caller="
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-mapping-forbids-ccac6b05c380]
kind = "forbids"
slice = "resolve-kernel-v1"
pattern = "resolver="
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-mapping-forbids-8af7d334f77c]
kind = "forbids"
slice = "resolve-kernel-v1"
pattern = "counterpart="
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-mapping-forbids-b8235f790307]
kind = "forbids"
slice = "resolve-kernel-v1"
pattern = "authority="
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-mapping-identity-forbids-a0ae78551ff2]
kind = "forbids"
slice = "resolve-kernel-v1"
pattern = "planner"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-mapping-identity-forbids-03bf655b44b4]
kind = "forbids"
slice = "resolve-kernel-v1"
pattern = "plan-quality-advisor"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-mapping-identity-forbids-3fc49d17e29b]
kind = "forbids"
slice = "resolve-kernel-v1"
pattern = "人間"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-caller-4fc44661467f]
kind = "requires"
slice = "resolve-kernel-v1"
pattern = "caller-owned boundary"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-scope-4490430b813e]
kind = "requires"
slice = "resolve-kernel-v1"
pattern = "caller 固有 mapping は各 caller Skill が所有し、この Kernel は caller role mapping を複製しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-scope-df59918bf5a8]
kind = "requires"
slice = "resolve-kernel-v1"
pattern = "共通 `resolve` Skill の新設は対象外である。"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-659e3baaa09e]
kind = "requires"
slice = "resolve-kernel-v1"
pattern = "workflow や orchestration ではなく"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-b0c06f109360]
kind = "requires"
slice = "resolve-kernel-v1"
pattern = "atomic one-at-a-time"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-queue-not-95739a24b3df]
kind = "requires"
slice = "resolve-kernel-v1"
pattern = "判断 queue を最初に固定しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-738731df6eee]
kind = "requires"
slice = "resolve-kernel-v1"
pattern = "resolve → apply → verify → semantic progress observation → frontier re-evaluation"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-updated-097ee501de21]
kind = "requires"
slice = "resolve-kernel-v1"
pattern = "semantic progress を確認した updated snapshot"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-184b818a82a2]
kind = "requires"
slice = "resolve-kernel-v1"
pattern = "verify failure reopen"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-no-53d607383d0c]
kind = "requires"
slice = "resolve-kernel-v1"
pattern = "latent な issue を新たに探索して frontier へ追加しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-2a39e45d560a]
kind = "requires"
slice = "resolve-kernel-v1"
pattern = "caller-owned bound"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-no-280b4a5a9889]
kind = "requires"
slice = "resolve-kernel-v1"
pattern = "no-progress cycle は進展とみなさず"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-frontier-not-4c1fa703fa75]
kind = "requires"
slice = "resolve-kernel-v1"
pattern = "frontier empty is not workflow completion"
applies_to = ["claude", "codex", "cursor"]

[contracts.resolve-kernel-no-new-public-ac24a1c14906]
kind = "requires"
slice = "resolve-kernel-v1"
pattern = "新しい workflow-level status、ledger schema、public parameter を所有・導入しない。"
applies_to = ["claude", "codex", "cursor"]
