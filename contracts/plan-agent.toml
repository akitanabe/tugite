[contracts.plan-candidate-producer-internal-guard-requires-86104357309e]
kind = "requires"
slice = "plan-candidate-producer-internal-guard"
pattern = "この Skill は plan-family public workflow parent の同じ context 内だけで使う internal skill であり、ユーザーから直接起動しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-internal-guard-requires-091dfa1ae647]
kind = "requires"
slice = "structural-health-gate-kernel-exclusion-intro"
pattern = '''明示起動された plan-family public workflow parent の同じ context 内だけで使い、単独起動、ユーザーからの直接起動、
plan-family 外の workflow からの流用はしない。'''
applies_to = ["claude", "codex", "cursor"]

# Why Not: loader fields must remain one relation; semantic gate assessment belongs to EVAL.
[contracts.shg-rmo-loader-data-453907002320]
kind = "requires"
slice = "structural-health-gate-rmo-parent-mapping"
pattern = '''path = ../../references/reality-model-observation-kernel.md
load_timing = after caller context is established and immediately before candidate assessment
identity = reality-model-observation-kernel-v1
required_sections = [Contract, Observable Reality Model, Method, Reintegration, Target Membership Check, Consumer Responsibilities, Non-goals]
failure = context-not-established
owner = structural-health-gate parent
delegate_path_resolution = false'''
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-rmo-fail-distinct-ef0e8708ffbe]
kind = "requires"
slice = "structural-health-gate-rmo-parent-mapping"
pattern = "loader failure は `context-not-established` として親の既存 routing で `stop-incomplete` へ返す。これは Reality evidence 不足の `insufficient-evidence` と区別する。"
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-rmo-viewpoint-b0a3c57f2d16]
kind = "requires"
slice = "structural-health-gate-rmo-parent-mapping"
pattern = "は Observation Viewpoint として使う。taxonomy を verdict にしない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-rmo-target-only-59b2413c6c8c]
kind = "requires"
slice = "structural-health-gate-rmo-parent-mapping"
pattern = "Target-relative Structural Problem だけを structural finding とする。"
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-rmo-local-incidental-06ff5dbd32e4]
kind = "requires"
slice = "structural-health-gate-rmo-parent-mapping"
pattern = "local wording / detail は Incidental Finding"
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-rmo-no-false-root-9037f2c34a49]
kind = "requires"
slice = "structural-health-gate-rmo-parent-mapping"
pattern = "同一因果を grounding できる surface だけを root problem に統合し、独立した problem を圧縮しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-boundary-viewpoint-8aa61411daa0]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = '''これらは Observation Viewpoint であり、
Kernel の taxonomy ではない。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-boundary-no-local-a39477597645]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = '''local wording / detail は Incidental Finding とし、
structural finding にしない。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-boundary-loader-distinct-89377816a5d4]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "これは loader failure の `context-not-established` と混同しない。"
applies_to = ["claude", "codex", "cursor"]

# Why Not: loader fields must remain one relation; order anchors separately protect meaningful timing.
[contracts.plan-candidate-producer-batch-loader-data-afb23bdae9ec]
kind = "requires"
slice = "plan-candidate-producer-batch-resolve-kernel-loader"
pattern = '''path = ../../references/batch-resolve-kernel.md
load_timing = once at plan-candidate-producer invocation start
identity = batch-resolve-kernel-v1
dependencies = none
required_sections = [適用モデル, Snapshot discipline, Resolution Transaction, Caller boundary]
failure = stop-incomplete
owner = plan-candidate-producer parent
delegate_path_resolution = false'''
applies_to = ["claude", "codex", "cursor"]
# Why Not: role, snapshot, point mapping, authority, and ledger are one caller mapping.
[contracts.plan-candidate-producer-batch-role-data-6003cbec86d3]
kind = "requires"
slice = "plan-candidate-producer-batch-resolution-role-mapping"
pattern = '''caller = plan-family public workflow parent
resolver = plan-candidate-producer planner
counterpart = plan-quality-advisor
target_snapshot = origin verified candidate snapshot
insight = Resolution Point
same_snapshot_insights = Resolution Batch
authority = discretionary | constrained
authority_constraints = immutable invocation Data injected by public parent
authority_source = injected Data only; never caller identity
ledger = adoption ledger'''
applies_to = ["claude", "codex", "cursor"]

[contracts.pcp-authority-no-caller-branch-requires-8385e9af73ff]
kind = "requires"
slice = "plan-candidate-producer-batch-resolution-role-mapping"
pattern = "caller 名から authority または振る舞いを選ばず"
applies_to = ["claude", "codex", "cursor"]

[contracts.pcp-constrained-validation-requires-e72d7c1b7ef3]
kind = "requires"
slice = "plan-candidate-producer-batch-resolution-role-mapping"
pattern = '''constrained_validation_timing = before working state / advisor / Resolution Transaction
constraint_failure_cases = [missing, invalid, ambiguous, duplicate, extra, identity regeneration, meaning-evidence mismatch]
constraint_failure = stop-incomplete before refinement'''
applies_to = ["claude", "codex", "cursor"]

[contracts.pcp-constraints-immutable-requires-fa14d78bcbee]
kind = "requires"
slice = "plan-candidate-producer-batch-resolution-role-mapping"
pattern = "immutable とし、plan-candidate-producer 内で再生成・追加・削除・置換しない"
applies_to = ["claude", "codex", "cursor"]
# Why Not: loader fields must remain one relation; semantic Claim adjudication belongs to EVAL.
[contracts.plan-candidate-producer-necessity-loader-data-4c92eb3a7096]
kind = "requires"
slice = "candidate-producer-boundary"
pattern = '''path = ../../references/necessity-kernel.md
load_timing = before candidate Claim adjudication
identity = necessity-kernel-v1
required_sections = [適用範囲, Task Specification, Claim と evidence, Deletion Test]
failure = stop-incomplete
owner = plan-candidate-producer parent
delegate_path_resolution = false'''
applies_to = ["claude", "codex", "cursor"]
# Why Not: loader fields must remain one relation; semantic RMO membership / adjudication belongs to EVAL.
[contracts.review-rmo-loader-data-5119909ac753]
kind = "requires"
slice = "review-refine-rmo-parent-mapping"
pattern = '''path = ../../references/reality-model-observation-kernel.md
load_timing = after normal reviewer result collection and before parent adjudication
identity = reality-model-observation-kernel-v1
required_sections = [Contract, Observable Reality Model, Method, Reintegration, Target Membership Check, Consumer Responsibilities, Non-goals]
failure = return-to-parent
owner = review-refine parent
delegate_path_resolution = false'''
applies_to = ["claude", "codex", "cursor"]
# Why Not: loader fields must remain one relation; semantic deletion adjudication belongs to EVAL.
[contracts.review-dtm-loader-data-448b9b95c48a]
kind = "requires"
slice = "review-refine-deletion-test-parent-mapping"
pattern = '''path = ../../references/deletion-test-method.md
load_timing = after final-trim reviewer result collection and before parent adjudication
identity = deletion-test-method-v1
required_sections = [Inputs, Procedure, Semantic Results, Non-goals]
failure = return-to-parent
owner = review-refine parent
delegate_path_resolution = false'''
applies_to = ["claude", "codex", "cursor"]

[contracts.review-rmo-no-auto-adopt-a63609c27bdf]
kind = "requires"
slice = "review-refine-rmo-parent-mapping"
pattern = '''Target-relative Derived Problem
だけを remediation Claim の候補にできるが、自動採用しない。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.review-rmo-incidental-oos-24853375e0d6]
kind = "requires"
slice = "review-refine-rmo-parent-mapping"
pattern = '''Incidental Finding は current modification
obligation にせず `out-of-scope` へ'''
applies_to = ["claude", "codex", "cursor"]

[contracts.review-rmo-finding-trigger-4fadfe7a1b69]
kind = "requires"
slice = "review-refine-rmo-parent-mapping"
pattern = '''通常 reviewer の observation は
Observation Trigger であり grounding evidence ではない。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.review-forbid-necessity-004788ae9925]
kind = "forbids"
slice = "review-refine-rmo-parent-mapping"
pattern = "necessity-kernel"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-dtm-selected-set-0400a908dde0]
kind = "requires"
slice = "review-refine-deletion-test-parent-mapping"
pattern = '''apply 前に
coherent selected deletion set 全体も一つの deletion candidate として origin / current verified snapshot に
対して再評価する。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.review-dtm-no-batch-witness-740cb38f24cb]
kind = "requires"
slice = "review-refine-deletion-test-parent-mapping"
pattern = '''個別 candidate の
評価で同じ selected Batch 内の別 candidate を stable remaining witness として扱わない。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.review-dtm-updated-snapshot-ded1751ea0e8]
kind = "requires"
slice = "review-refine-deletion-test-parent-mapping"
pattern = "更新後は新しい snapshot で再判定する。"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-forbid-old-preflight-name]
kind = "forbids"
pattern = "review-kernel-preflight"
paths = [
  "plugins/claude/skills/review-refine/SKILL.md",
  "plugins/codex/skills/review-refine/SKILL.md",
  "plugins/cursor/skills/review-refine/SKILL.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.review-ledger-grounding-9b558451510d]
kind = "requires"
slice = "review-refine-batch-resolve-kernel-parent-mapping"
pattern = '''Target、grounding trace、observation / inference、
membership、attribution を再現可能な形で残す。'''
applies_to = ["claude", "codex", "cursor"]

# Why Not: loader fields must remain one relation; the order anchor protects meaningful timing.
[contracts.review-batch-loader-data-bab77a47fc0f]
kind = "requires"
slice = "review-refine-batch-resolve-kernel-parent-mapping"
pattern = '''path = ../../references/batch-resolve-kernel.md
load_timing = before first Resolution Transaction
identity = batch-resolve-kernel-v1
dependencies = none
required_sections = [適用モデル, Snapshot discipline, Resolution Transaction, Caller boundary]
failure = stop-incomplete or review-not-established
owner = review-refine parent
delegate_path_resolution = false'''
applies_to = ["claude", "codex", "cursor"]
# Why Not: role, snapshot, finding mapping, authority, and ledger are one caller mapping.
[contracts.review-batch-role-data-2a7c9404400d]
kind = "requires"
slice = "review-refine-batch-resolve-kernel-parent-mapping"
pattern = '''caller = review-refine parent
resolver = review-refine parent
counterpart = reviewer
target_snapshot = origin verified artifact snapshot
finding = Resolution Point
same_snapshot_findings = Resolution Batch
authority = parent-owned
ledger = finding ledger and hold ledger'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-local-planner-requires-f4ae8dc4567f]
kind = "requires"
slice = "candidate-producer-boundary"
pattern = "plan-candidate-producer planner"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-local-review-refine-forbids-da0a88391a40]
kind = "forbids"
slice = "candidate-producer-boundary"
pattern = "review-refine"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-resolution-ownership-requires-16f105f55c04]
kind = "requires"
slice = "plan-agent-proposal-resolution-ownership"
pattern = "plan-candidate-producer の invocation boundary、discretionary authority、resolution execution bound は `plan-agent` が所有する"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-resolve-loader-before-use]
kind = "order"
before = "plan-candidate-producer-batch-resolve-kernel-loader-start"
after = "plan-candidate-producer-batch-resolve-kernel-use"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-resolve-loader-before-cycle]
kind = "order"
before = "plan-candidate-producer-batch-resolve-kernel-loader-start"
after = "plan-candidate-producer-batch-resolution-transaction-start"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-resolve-loader-before-verified-baseline]
kind = "order"
before = "plan-candidate-producer-batch-resolve-kernel-loader-start"
after = "plan-candidate-producer-verified-baseline-start"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-resolution-before-bounded-return]
kind = "order"
before = "plan-candidate-producer-batch-resolution-transaction-start"
after = "plan-candidate-producer-bounded-return-start"
applies_to = ["claude", "codex", "cursor"]

[contracts.pcp-caller-verified-snapshot-requires-59817ba4e57e]
kind = "requires"
slice = "plan-candidate-producer-verified-snapshot-baseline"
pattern = "既存 candidate を受け取る場合は current verified candidate snapshot"
applies_to = ["claude", "codex", "cursor"]

[contracts.pcp-existing-s0-no-redraft-requires-395e7bfe8d6a]
kind = "requires"
slice = "plan-candidate-producer-verified-snapshot-baseline"
pattern = "current verified candidate snapshot を S0 とし、一から再起草せず"
applies_to = ["claude", "codex", "cursor"]

[contracts.pcp-initial-verified-snapshot-requires-dd6895e9b3e9]
kind = "requires"
slice = "plan-candidate-producer-verified-snapshot-baseline"
pattern = "verify の成功と semantic progress の確認後にだけ、初期 current verified snapshot"
applies_to = ["claude", "codex", "cursor"]

[contracts.pcp-improvement-working-state-requires-665ef82ed9f3]
kind = "requires"
slice = "plan-candidate-producer-verified-snapshot-baseline"
pattern = "各改善は working state へ apply"
applies_to = ["claude", "codex", "cursor"]

[contracts.pcp-working-state-promotion-requires-e9e050aa7d46]
kind = "requires"
slice = "plan-candidate-producer-verified-snapshot-baseline"
pattern = "verify と semantic progress が成功した後にだけ current verified snapshot を更新する"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-resolution-mapping-before-plan-candidate-producer]
kind = "order"
before = "plan-agent-proposal-resolution-caller-mapping"
after = "plan-candidate-producer-start"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-batch-origin-9ece84d83a60]
kind = "requires"
slice = "plan-candidate-producer-batch-resolution-transaction"
pattern = "同じ origin verified candidate snapshot に束縛され transaction 開始時までに固定された Resolution"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-batch-fixed-membership-4dde1592b0d1]
kind = "requires"
slice = "plan-candidate-producer-batch-resolution-transaction"
pattern = "transaction 中に Batch membership を追加せず、snapshot 更新後に frontier を\n再計算しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-batch-adjudicate-all-7666a0af4b44]
kind = "requires"
slice = "plan-candidate-producer-batch-resolution-transaction"
pattern = "mutation 前に Batch 全体を裁定し、conflict / dependency を解消する"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-batch-coherent-apply-e2506bf9a897]
kind = "requires"
slice = "plan-candidate-producer-batch-resolution-transaction"
pattern = "selected set を原則 single partition の coherent revision として apply"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-batch-promotion-6e35ba6ce11f]
kind = "requires"
slice = "plan-candidate-producer-batch-resolution-transaction"
pattern = "verify と caller-owned semantic progress が成功するまで current verified snapshot に promote しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-batch-isolate-60dc167f5514]
kind = "requires"
slice = "plan-candidate-producer-batch-resolution-transaction"
pattern = "同じ isolation baseline から `isolate` して安全に処理できる subset"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-batch-corrective-ef4a3c0ee6b1]
kind = "requires"
slice = "plan-candidate-producer-batch-resolution-transaction"
pattern = "元 Batch の point への corrective\nadjudication"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-batch-boundary-fd61f48dfb27]
kind = "requires"
slice = "plan-candidate-producer-batch-resolution-transaction"
pattern = "authority または evidence が\n不足する point は推測で selected set に含めず"
applies_to = ["claude", "codex", "cursor"]

# Why Not: this transition block is structured execution Data; line order carries the two-pass sequence.
[contracts.plan-candidate-producer-two-pass-flow-216037f1abda]
kind = "requires"
slice = "advisor-two-pass-orchestration"
pattern = "candidate S0\n→ fresh-context advisor #1\n→ Resolution Batch #1\n→ Resolution Transaction #1"
applies_to = ["claude", "codex", "cursor"]

# Why Not: this transition block is structured execution Data; line order carries the second-pass sequence.
[contracts.plan-candidate-producer-two-pass-second-6dff8d8b527a]
kind = "requires"
slice = "advisor-two-pass-orchestration"
pattern = "verified candidate S1\n→ fresh-context advisor #2\n→ Resolution Batch #2\n→ Resolution Transaction #2"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-exactly-aa0e66d407b3]
kind = "requires"
slice = "advisor-two-pass-orchestration"
pattern = "exactly 2 pass"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-empty-94c3e1372740]
kind = "requires"
slice = "advisor-two-pass-orchestration"
pattern = "Batch または selected set が空でも第2 passを必ず起動"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-no-third-5d4af6f803a7]
kind = "requires"
slice = "advisor-two-pass-orchestration"
pattern = "第3 passを起動しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-scope-34d7e176a74d]
kind = "requires"
slice = "plan-candidate-producer-two-advisor-passes"
pattern = "fulfillment check"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-induced-ed1c12ebb08d]
kind = "requires"
slice = "plan-candidate-producer-two-advisor-passes"
pattern = "revision-induced issue"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-contest-570bf86d67fe]
kind = "requires"
slice = "plan-candidate-producer-two-advisor-passes"
pattern = "rejected contest"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-unresolved-86343deaa9d1]
kind = "requires"
slice = "plan-candidate-producer-two-advisor-passes"
pattern = "unresolved revisit"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-data-99ff6abc3e74]
kind = "requires"
slice = "plan-candidate-producer-two-advisor-passes"
pattern = "planner の `fully satisfied` 結論や fulfillment check に不要な adopted 理由を advisor の観測 context に含めず"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-no-output-943bf721cfdd]
kind = "requires"
slice = "plan-candidate-producer-two-advisor-passes"
pattern = "専用の output schema は新設せず既存の non-binding output boundary を維持する"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-empty-batch-41351c593a6e]
kind = "requires"
slice = "advisor-two-pass-orchestration"
pattern = "該当しなければ空 Batch として第2 Transaction を閉じる"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-resolution-bound-fixed-requires-ea344b270245]
kind = "requires"
slice = "plan-agent-proposal-resolution-ownership"
pattern = "plan-candidate-producer loop の開始時に resolution execution bound を internal responsibility"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-no-resolution-surface-requires-900b81bf5714]
kind = "requires"
slice = "plan-agent-proposal-resolution-ownership"
pattern = "固定数、新しい public parameter / schema、`resolve_rounds` は追加しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-resolution-budget-separation-requires-1b20120c8c86]
kind = "requires"
slice = "plan-agent-proposal-resolution-ownership"
pattern = "structural gate budget および review-refine budget と別 Data"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-batch-stop-boundary-7ed850142249]
kind = "requires"
slice = "plan-candidate-producer-batch-resolution-transaction"
pattern = "独立して処理できる point を止めずに caller-owned boundary へ返す"
applies_to = ["claude", "codex", "cursor"]

[contracts.pcp-verified-present-return-requires-10983b3123f1]
kind = "requires"
slice = "plan-candidate-producer-bounded-return"
pattern = "verified_present_return = candidate snapshot identity + constraint-compliant completed partitions + verification evidence"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-absent-return-requires-40985d991870]
kind = "requires"
slice = "plan-candidate-producer-bounded-return"
pattern = "absent_return = candidate_snapshot absent + empty completed partitions + no fabricated identity"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-restart-baseline-requires-ec9da4a535e1]
kind = "requires"
slice = "plan-candidate-producer-bounded-return"
pattern = "restart_baseline = verified promoted state only; exclude unverified or non-promoted working state"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-authority-conflict-requires-9f245a63b67e]
kind = "requires"
slice = "plan-candidate-producer-bounded-return"
pattern = "`authority_conflict`\npacket"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-batch-loader-before-transaction]
kind = "order"
before = "review-refine-batch-loader-start"
after = "review-refine-resolution-transaction-start"
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-kernel-intro-forbids-910ca2f74f89]
kind = "forbids"
slice = "structural-health-gate-kernel-exclusion-intro"
pattern = "necessity-kernel"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-caller-context-forbids-901b6b82f7ce]
kind = "forbids"
slice = "structural-health-gate-caller-context"
pattern = "necessity-kernel"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-forbids-1e6e10a9e138]
kind = "forbids"
slice = "structural-health-gate-boundary"
pattern = "necessity-kernel"
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-kernel-output-forbids-8380a4be1c8d]
kind = "forbids"
slice = "structural-health-gate-kernel-exclusion-output"
pattern = "necessity-kernel"
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-intro-plan-agent-forbids-201436250f20]
kind = "forbids"
slice = "structural-health-gate-kernel-exclusion-intro"
pattern = "plan-agent"
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-intro-review-refine-forbids-ca025245b59a]
kind = "forbids"
slice = "structural-health-gate-kernel-exclusion-intro"
pattern = "review-refine"
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-caller-plan-agent-forbids-30e0e582e3d3]
kind = "forbids"
slice = "structural-health-gate-caller-context"
pattern = "plan-agent"
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-caller-review-refine-forbids-d8b5847dc570]
kind = "forbids"
slice = "structural-health-gate-caller-context"
pattern = "review-refine"
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-boundary-plan-agent-forbids-ed7645ce1832]
kind = "forbids"
slice = "structural-health-gate-boundary"
pattern = "plan-agent"
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-boundary-review-refine-forbids-2723bcd5fcce]
kind = "forbids"
slice = "structural-health-gate-boundary"
pattern = "review-refine"
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-output-plan-agent-forbids-6aa1b23472d3]
kind = "forbids"
slice = "structural-health-gate-kernel-exclusion-output"
pattern = "plan-agent"
applies_to = ["claude", "codex", "cursor"]

[contracts.shg-output-review-refine-forbids-499097e33edd]
kind = "forbids"
slice = "structural-health-gate-kernel-exclusion-output"
pattern = "review-refine"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-kernel-reference-before-criteria]
kind = "order"
before = "plan-candidate-producer-kernel-reference"
after = "plan-candidate-producer-kernel-criteria"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-rmo-reference-before-criteria]
kind = "order"
before = "review-refine-rmo-reference"
after = "review-refine-rmo-criteria"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-dtm-reference-before-criteria]
kind = "order"
before = "review-refine-deletion-test-reference"
after = "review-refine-deletion-test-criteria"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-rmo-before-batch]
kind = "order"
before = "review-refine-rmo-criteria"
after = "review-refine-resolution-transaction-start"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-adjudication-requires-6121b42635ea]
kind = "requires"
slice = "review-refine-parent-adjudication-vocabulary"
pattern = "parent-owned adjudication/result values are exactly `adopted`, `rejected`, `out-of-scope`, `deferred`, and `human-confirmation`"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-adjudication-requires-571e577a9083]
kind = "requires"
slice = "review-refine-parent-adjudication-vocabulary"
pattern = "`adopted`: 成果物を修正して verification する"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-adjudication-requires-ae56858f26ac]
kind = "requires"
slice = "review-refine-parent-adjudication-vocabulary"
pattern = "`rejected`: 既存仕様または evidence に基づき修正しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-adjudication-requires-24431cd54040]
kind = "requires"
slice = "review-refine-parent-adjudication-vocabulary"
pattern = "`out-of-scope`: 成立性は否定しないが対象外として残存事項へ渡す"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-adjudication-requires-053f70479137]
kind = "requires"
slice = "review-refine-parent-adjudication-vocabulary"
pattern = "`deferred`: 仕様未決、記載漏れ、誤認、対象外、情報不足のいずれかを確定できないため凍結する"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-adjudication-requires-e1b2aa6a14f0]
kind = "requires"
slice = "review-refine-parent-adjudication-vocabulary"
pattern = "`human-confirmation`: 実装・公開・互換性など、親だけでは決められない確認を要求する"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-adjudication-forbids-1ee250fa2e7d]
kind = "forbids"
slice = "review-refine-parent-adjudication-vocabulary"
pattern = "`採用`"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-adjudication-forbids-4d1d79112b12]
kind = "forbids"
slice = "review-refine-parent-adjudication-vocabulary"
pattern = "`却下`"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-adjudication-forbids-84e3ec9cc1f4]
kind = "forbids"
slice = "review-refine-parent-adjudication-vocabulary"
pattern = "`範囲外`"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-adjudication-forbids-ae92875b4e92]
kind = "forbids"
slice = "review-refine-parent-adjudication-vocabulary"
pattern = "`判断保留`"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-adjudication-forbids-d361763b89a3]
kind = "forbids"
slice = "review-refine-parent-adjudication-vocabulary"
pattern = "`人間確認`"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-adjudication-forbids-e1f760aed3c0]
kind = "forbids"
slice = "review-refine-parent-adjudication-vocabulary"
pattern = "adjudication=accepted"
applies_to = ["claude", "codex", "cursor"]

[contracts.candidate-producer-boundary-requires-00d2b5fa97bf]
kind = "requires"
slice = "candidate-producer-boundary"
pattern = "candidate snapshot"
applies_to = ["claude", "codex", "cursor"]

[contracts.candidate-producer-boundary-requires-970238747749]
kind = "requires"
slice = "candidate-producer-boundary"
pattern = "stop-incomplete"
applies_to = ["claude", "codex", "cursor"]

[contracts.candidate-producer-boundary-requires-d94b9dfcc3e4]
kind = "requires"
slice = "candidate-producer-boundary"
pattern = "adopted"
applies_to = ["claude", "codex", "cursor"]

[contracts.candidate-producer-boundary-requires-510ffcd38eb2]
kind = "requires"
slice = "candidate-producer-boundary"
pattern = "rejected"
applies_to = ["claude", "codex", "cursor"]

[contracts.candidate-producer-boundary-requires-782a3cd459ab]
kind = "requires"
slice = "candidate-producer-boundary"
pattern = "unresolved"
applies_to = ["claude", "codex", "cursor"]

[contracts.candidate-producer-boundary-requires-c3d19dfdd3b4]
kind = "requires"
slice = "plan-candidate-producer-bounded-return"
pattern = "caller-owned parent"
applies_to = ["claude", "codex", "cursor"]

[contracts.candidate-producer-boundary-requires-fa737d884a52]
kind = "requires"
slice = "plan-candidate-producer-bounded-return"
pattern = "後段工程を選択・起動せず"
applies_to = ["claude", "codex", "cursor"]

[contracts.candidate-producer-boundary-requires-7104fc6ef7f0]
kind = "requires"
slice = "candidate-producer-boundary"
pattern = "advisor insight を自動採用せず"
applies_to = ["claude", "codex", "cursor"]

[contracts.candidate-producer-boundary-requires-bd3960ba6cda]
kind = "requires"
slice = "candidate-producer-boundary"
pattern = "planner は各 insight を一次情報と要求に照らして次の台帳へ裁定する"
applies_to = ["claude", "codex", "cursor"]

[contracts.candidate-producer-boundary-requires-06fd6dd36446]
kind = "requires"
slice = "plan-candidate-producer-bounded-return"
pattern = "具体的な品質向上が残る間だけ bounded"
applies_to = ["claude", "codex", "cursor"]

[contracts.candidate-producer-boundary-requires-2453dc7e6ef4]
kind = "requires"
slice = "plan-candidate-producer-bounded-return"
pattern = "安全な candidate を推奨できない場合"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-return-after-two-pass-8a51622c6e33]
kind = "requires"
slice = "plan-candidate-producer-bounded-return"
pattern = "Resolution Transaction を完了し"
applies_to = ["claude", "codex", "cursor"]

[contracts.candidate-producer-boundary-requires-71c110088df1]
kind = "requires"
slice = "candidate-producer-boundary"
pattern = "`structural-health-gate` の意味は この mapping の対象外である"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-quality-advisor-boundary-requires-cad74d5c8b39]
kind = "requires"
slice = "plan-quality-advisor-boundary"
pattern = "read-only advisor"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-quality-advisor-boundary-requires-5cb8d973e9fb]
kind = "requires"
slice = "plan-quality-advisor-boundary"
pattern = "candidate を直接修正せず"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-quality-advisor-boundary-requires-6ce252832167]
kind = "requires"
slice = "plan-quality-advisor-boundary"
pattern = "第二の planner にならない"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-quality-advisor-boundary-requires-eeecc60d1487]
kind = "requires"
slice = "plan-quality-advisor-boundary"
pattern = "後段を起動せず"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-quality-advisor-boundary-requires-866a7bae958a]
kind = "requires"
slice = "plan-quality-advisor-boundary"
pattern = "採否を決めず"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-quality-advisor-boundary-requires-3a3dfce765ab]
kind = "requires"
slice = "plan-quality-advisor-boundary"
pattern = "最終受入を行いません"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-quality-advisor-no-human-route-requires-d063f8b1487a]
kind = "requires"
slice = "plan-quality-advisor-boundary"
pattern = "人間への質問、選択肢、承認要求、仲裁経路を作りません。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-quality-advisor-planner-input-requires-ca61b8d840c1]
kind = "requires"
slice = "plan-quality-advisor-boundary"
pattern = "`question_or_option` は planner 専用の裁定入力"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-plan-candidate-producer-handoff-requires-6440cedf2954]
kind = "requires"
slice = "plan-agent-plan-candidate-producer-handoff"
pattern = "candidate snapshot"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-plan-candidate-producer-handoff-requires-c2e7ac715ad2]
kind = "requires"
slice = "plan-agent-plan-candidate-producer-handoff"
pattern = "stop-incomplete"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-plan-candidate-producer-handoff-requires-cf935772edbf]
kind = "requires"
slice = "plan-agent-plan-candidate-producer-handoff"
pattern = "caller-owned parent"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-plan-candidate-producer-before-review-refine]
kind = "order"
before = "plan-candidate-producer-start"
after = "review-refine-handoff"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-plan-candidate-producer-before-stop-boundary]
kind = "order"
before = "plan-candidate-producer-start"
after = "plan-candidate-producer-stop-boundary"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-a1d910d24911]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "duplicated source of truth"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-caller-context-requires-8d62d9386d3e]
kind = "requires"
slice = "structural-health-gate-caller-context"
pattern = "`caller_context` Data"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-caller-context-requires-da0a25ead2cb]
kind = "requires"
slice = "structural-health-gate-caller-context"
pattern = "`workflow_family`: `plan-family`"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-caller-context-requires-8724c05250c8]
kind = "requires"
slice = "structural-health-gate-caller-context"
pattern = "`invocation`: `explicit-public-parent`"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-caller-context-requires-e7dbd70e7e92]
kind = "requires"
slice = "structural-health-gate-caller-context"
pattern = "context 不成立"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-caller-context-requires-1b5ebae10399]
kind = "requires"
slice = "structural-health-gate-caller-context"
pattern = "assessment を開始せず"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-caller-context-requires-620dfbff0cb9]
kind = "requires"
slice = "structural-health-gate-caller-context"
pattern = "`stop-incomplete`"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-caller-context-forbids-0067e3d70daf]
kind = "forbids"
slice = "structural-health-gate-caller-context"
pattern = "`producer_context`"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-caller-context-forbids-1e9c62107d8d]
kind = "forbids"
slice = "structural-health-gate-caller-context"
pattern = "`return target`"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-caller-context-forbids-d1adb9637f59]
kind = "forbids"
slice = "structural-health-gate-caller-context"
pattern = "`next skill`"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-caller-context-forbids-bf899db2ca07]
kind = "forbids"
slice = "structural-health-gate-caller-context"
pattern = "`origin`"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-b344367737df]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "明示起動された plan-family public workflow parent"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-15d0e8d8bbd1]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "`location`"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-8e53ba15aa04]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "`non_local_reason`"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-151a3e65ea4d]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "`predicted_amplification`"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-7c2e0122fbd5]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "`predicted_churn`"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-730ad200ba29]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "public workflow parent が最終的な `pass` / `return` / `stop-incomplete` を決める"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-c0dfb8011b7d]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "成果物を再設計・直接編集しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-33c91c95d9df]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "長さ、複雑さ、finding 数だけを理由に `return` しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-f3fe4560d93b]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "`insufficient-evidence`"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-f3bc6f27cedf]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "assessment Data"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-7219f9a397db]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "未解決の方向性または責務が、新しい設計判断を後段へ要求していないか"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-cdeee4c6f1ae]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "requirements、design、Acceptance Criteria、verification の対応漏れまたは矛盾"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-abb74d2469d2]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "用語、state、priority、responsibility の定義または遷移の不整合"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-8db83613e288]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "局所修正が他の要件、責務、成果物全体へ広く波及する ripple"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-bc1848ab57d3]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "複数 finding に見える問題が、一つの structural defect から派生していないか"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-a7aaad293999]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "例外、停止条件、stop contract の追加が増殖し、共通責務の欠落を覆っていないか"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-65aeab3fcd1c]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "局所修正で閉じる密度、詳細不足、文章上の重複は 通常の review で扱えるため、構造欠陥の evidence と混同しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-requires-96417dda489a]
kind = "requires"
slice = "structural-health-gate-boundary"
pattern = "reviewer または advisor を使う場合、その出力は evidence のみであり、candidate の採否、修正、再起草、工程の終了を決めさせない"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-forbids-30802345d071]
kind = "forbids"
slice = "structural-health-gate-boundary"
pattern = "gate が最終判断する"
applies_to = ["claude", "codex", "cursor"]

[contracts.structural-health-gate-boundary-forbids-bf56e19cfba7]
kind = "forbids"
slice = "structural-health-gate-boundary"
pattern = "gate が成果物を直接編集する"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-structural-health-handoff-requires-b2931e80ed6a]
kind = "requires"
slice = "plan-agent-structural-health-handoff"
pattern = "`structural-health-gate`"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-structural-health-handoff-requires-87af65d49afa]
kind = "requires"
slice = "plan-agent-structural-health-handoff"
pattern = "親は candidate snapshot、gate assessment、gate evidence、current round、producer status を確定 Data として `candidate-structural-gate-routing` Flow へ渡す。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-structural-health-handoff-requires-856a4e49ebf9]
kind = "requires"
slice = "plan-agent-structural-health-handoff"
pattern = "親は structural gate の意味を裁定し、必要な candidate の準備・修正・採否を保持する。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-structural-health-handoff-requires-59e5490b4224]
kind = "requires"
slice = "plan-agent-structural-health-handoff"
pattern = "未指定時の valid"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-structural-boundary-requires-e59ac3fcd3cf]
kind = "requires"
slice = "review-refine-structural-boundary"
pattern = "局所的な構造欠陥を事前解消"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-structural-boundary-requires-d4cb267dcd67]
kind = "requires"
slice = "review-refine-structural-boundary"
pattern = "自動で `structural-health-gate` または candidate producer へ逆走しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-structural-boundary-requires-9a09b477dcd4]
kind = "requires"
slice = "review-refine-structural-boundary"
pattern = "`stop-incomplete`"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-induced-brake-requires-7bf1a9955bf6]
kind = "requires"
slice = "review-refine-induced-brake"
pattern = "直前までに親が採用して verification を完了した修正によって finding が新たに成立した"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-induced-brake-requires-3bd37375df36]
kind = "requires"
slice = "review-refine-induced-brake"
pattern = "対象文の新旧、snapshot 差分、同じ指摘の再出現だけでは"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-induced-brake-requires-e5f14ad38e07]
kind = "requires"
slice = "review-refine-induced-brake"
pattern = "各通常 round で、親確定の `修正推奨` 以上"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-induced-brake-requires-1c57f79b6316]
kind = "requires"
slice = "review-refine-induced-brake"
pattern = "通常 round 2 回で連続して"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-induced-brake-requires-a715ae3d4a85]
kind = "requires"
slice = "review-refine-induced-brake"
pattern = "両 round とも非誘発の `修正必須` が 0"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-induced-brake-requires-8b5582848464]
kind = "requires"
slice = "review-refine-induced-brake"
pattern = "`induced-loop` は自己誘発 churn に対する補助ブレーキ"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-induced-brake-forbids-6db5d7a6cc67]
kind = "forbids"
slice = "review-refine-induced-brake"
pattern = "`baseline_round`"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-plan-candidate-producer-before-structural-health-gate]
kind = "order"
before = "plan-candidate-producer-start"
after = "structural-health-gate-start"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-routing-requires-a2534dc79b9a]
kind = "requires"
slice = "plan-agent-review-routing"
pattern = "まず `artifact_kind` と既定 `plan-adversarial-reviewer` の責務から、成果物が reviewer 適用対象かを親が判定し、確認済み applicability を Flow に渡す。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-goal-requires-4f5185a699b2]
kind = "requires"
slice = "plan-agent-review-routing"
pattern = "`review_goal` は、ユーザー指定の review goal や追加の具体的な risk がない場合、「実装前プランの具体的な failure path を確認し、確定候補にできるか判断する」とする。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-goal-requires-fe2353d05d2c]
kind = "requires"
slice = "plan-agent-review-routing"
pattern = "これは plan review 自体の既定目的であり、毎回 risk を事前発見することを要求しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-goal-requires-28a2f6d032b1]
kind = "requires"
slice = "plan-agent-review-routing"
pattern = "ユーザー指定 goal や追加 risk は既存 reviewer の責務内で追加できる。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-goal-forbids-8b0463b5cd07]
kind = "forbids"
slice = "plan-agent-review-routing"
pattern = "追加 risk がなければ `review_goal` を設定しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-routing-parent-a4a043497f34]
kind = "requires"
slice = "plan-agent-review-routing"
pattern = "適用対象なら、ユーザーの review 省略指定を親が確認し、確認済み opt-out を applicability とともに Flow に渡す。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-routing-parent-da227491563f]
kind = "requires"
slice = "plan-agent-review-routing"
pattern = "readiness Data として Flow に渡す。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-routing-requires-943449028bc2]
kind = "requires"
slice = "plan-agent-review-routing"
pattern = "review-refine の結果を `adopted` / `rejected` / `out-of-scope` /\n`deferred` / `human-confirmation` のいずれかへ evidence・理由とともに裁定して保持する"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-routing-requires-70f414885590]
kind = "requires"
slice = "plan-agent-review-routing"
pattern = "`deferred` は既存の hold ledger を\n通じて次回入力へ渡し、loop 中は凍結する"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-routing-forbids-88dde65c9c5b]
kind = "forbids"
slice = "plan-agent-review-routing"
pattern = "review-refine の5区分（採用、却下、範囲外、判断保留、\n人間確認）"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-structural-health-gate-before-review-refine]
kind = "order"
before = "structural-health-gate-start"
after = "review-refine-handoff"
applies_to = ["claude", "codex", "cursor"]

[contracts.public-workflow-routing-requires-26a8ef96a8ed]
kind = "requires"
slice = "public-workflow-routing"
pattern = "`return target` は public workflow parent が所有する"
applies_to = ["claude", "codex", "cursor"]

[contracts.public-workflow-routing-requires-c0c771074dde]
kind = "requires"
slice = "public-workflow-routing"
pattern = "明示されていない別 workflow へ自動 switch しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-plan-family-caller-requires-2b9e86d38e53]
kind = "requires"
slice = "review-refine-plan-family-caller"
pattern = "plan-family public workflow"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-plan-family-caller-requires-19b6ab0da4c9]
kind = "requires"
slice = "review-refine-plan-family-caller"
pattern = "public workflow parent"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-plan-family-caller-requires-b9d2bfc2d2e2]
kind = "requires"
slice = "review-refine-plan-family-caller"
pattern = "ユーザー明示の単独 review"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-plan-family-caller-requires-1ce9400e180e]
kind = "requires"
slice = "review-refine-plan-family-caller"
pattern = "workflow 間も自動で切り替えない"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-6092e7a56241]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "通常の `plan-agent` は、Human が途中経過を追わず Agent に任せる「おまかせ」workflow"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-07b1483cbe17]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "recommendation-first"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-62551ec33763]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "要求原文・repository evidence・scope・constraints・verification・risk"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-d19bb2bc09bd]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "不可約な意味衝突だけであり"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-33c2a98e4e16]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "要求は解釈するが"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-3542b13b9eed]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "authority/freeze とせず"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-a8559f23fef1]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "ユーザー要求、明示制約、観測事実は覆さず"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-c733f67769b9]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "人間確認` は reviewer が観測した signal に留まり"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-26a903a59a9c]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "現 candidate を推奨不能な状態"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-8ddcc3e61d7c]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "単なる複数案、別案、軽い不確実性だけでは"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-00fdc0103c88]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "別軸である"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-2f5711453ee1]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "blocking finding、未反映修正必須、要求意味変更、"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-077ffe21f4a1]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "read-only `plan-quality-advisor` を起動できる"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-1294aebbdce3]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "追加 round の期待利益"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-49d40ad34ade]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "final summary は明示 opt-out できる"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-be461baa1867]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "direction freeze / final acceptance は"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-cf5725b49a56]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "public workflow parent が内部工程として `final-candidate` / `incomplete` と圧縮出力を裁定し、"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-0400ab3b2013]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "summary opt-out は Artifact だけを返す"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-d7b1948b189e]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "Blocking Reason、Residual Risk を必ず返し"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-42d54df67c54]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "advisor には request、repository_observation、review_goal、current snapshot、finding ledger、直近差分、verification、residual risk を Data として渡す。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-3712bf2e2707]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "途中の decision / review 全履歴を Human に"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-autonomy-requires-5761e53e42d3]
kind = "requires"
slice = "plan-agent-autonomy"
pattern = "candidate status は次の Calculation で一度だけ決める。"
applies_to = ["claude", "codex", "cursor"]

# Why Not: consumer eligibility, artifact purity, and projection stay with plan-agent while publication is delegated to the shared reference.
[contracts.plan-agent-artifact-completion-35499369affc]
kind = "requires"
slice = "plan-agent-artifact-completion"
pattern = '''workflow = plan-agent
artifact_eligibility = semantic completion eligibility and verified publication_target
artifact_body = frozen final candidate body only
artifact_excludes = [Semantic Delta, Verification Delta, Human Attention, gate result, review result, decision ledger, finding ledger, process history]
pre_publication_artifact = none when incomplete
stdout = Result, short Summary, optional Human Attention, Artifact local path'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-publication-loader-978b2066f836]
kind = "requires"
slice = "plan-agent-local-artifact-completion"
pattern = '''publication_reference = ../../references/plan-artifact-publication.md
publication_load_timing = once before programmatic-publication use
publication_identity = plan-artifact-publication-v1
publication_use = parent-confirmed publication_target -> programmatic-publication -> outward status/stdout projection'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-publication-before-outcome]
kind = "order"
before = "plan-agent-publication-loader"
after = "plan-agent-publication-outcomes"
applies_to = ["claude", "codex", "cursor"]

# Why Not: dual load, Git predicate scope, and consumer 3-value must stay coherent; isolated loader presence could hide missing selection mapping.
[contracts.plan-agent-destination-selection-bcd9020a1200]
kind = "requires"
slice = "plan-agent-destination-selection"
pattern = '''destination_reference = ../../references/destination-selection.md
destination_load_timing = once before Agentic destination confirmation
destination_identity = destination-selection-v1
destination_required_sections = [Inputs / Outputs, Candidate facts, caller_owned_predicates の適用範囲, Programmatic Flows, Agentic unique selection, destination-reselection]
destination_failure = existing incomplete path; no new status'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-destination-selection-01071454baf2]
kind = "requires"
slice = "plan-agent-destination-selection"
pattern = "Git ignored/index predicates を project-local および canonical path が repository 内に入る OS-temp にだけ適用する範囲で渡す。repository 外の verified OS-temp に Git predicates を適用しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-destination-selection-dafa881f0e95]
kind = "requires"
slice = "plan-agent-destination-selection"
pattern = '''1. explicit 確定 → それを使う
2. qualified set のときだけ unique selection 本文を適用する
3. explicit incomplete および入力不足 incomplete → unique selection を起動せず incomplete。`publication_target` を組まない'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-destination-selection-e13691d15861]
kind = "requires"
slice = "plan-agent-destination-selection"
pattern = "`destination-reselection-required` を受けたとき、元が Human explicit なら同じ requested_destination を保持し unique-best auto-select へ落とさない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-destination-selection-27a041ffeb28]
kind = "requires"
slice = "plan-agent-destination-selection"
pattern = "decoy 一覧 HOW と ranking procedure をこの Skill に置かない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-no-retired-human-save-authority]
kind = "forbids"
pattern = "最終成果物の採用と保存・後続 Action の許可は Human が判断する"
applies_to = ["claude", "codex", "cursor"]

# Why Not: Flow fields and routing order are deterministic execution Data; keep the parent-owned semantic judgment outside the Flow.
[contracts.plan-candidate-producer-programmatic-boundary-975e2f84e2df]
kind = "requires"
slice = "plan-candidate-producer-programmatic-flows"
pattern = "Flow の procedure、条件、outcome は固定であり、Agent は override、bypass、置換しない。outcome の後に複数の妥当な Action が残る意味判断は Agentic な親へ返す。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-preflight-flow-ed264820436f]
kind = "requires"
slice = "producer-invocation-preflight"
pattern = '''Trigger: 親が plan-candidate-producer invocation を開始し、固定 preflight の実行を要求したとき。
Inputs: 親が注入した batch-resolve-kernel v1 と necessity-kernel v1 の Loader Data、authority、authority_constraints、candidate Claim 判定前の一次情報。
Procedure: batch-resolve-kernel loader を invocation start に一度だけ検証し、必要本文と identity / required_sections / failure / owner / delegate_path_resolution を確認する。batch の検証後も necessity-kernel loader は独立して、candidate Claim adjudication の前に検証する。両 loader の load timing と mapping は相互の load 順に依存させず、authority の列挙 / shape / identity / traceability は fixed validation として扱う。candidate content と Claim の採否・意味判断をこの Flow に入れない。
Outcomes: 検証済み preflight Data、または `stop-incomplete`。loader / authority failure は突破せず、candidate content と Claim の意味判断は Agentic な親へ返す。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-flow-0136ea811d0c]
kind = "requires"
slice = "advisor-two-pass-orchestration"
pattern = '''candidate S0
→ fresh-context advisor #1
→ Resolution Batch #1
→ Resolution Transaction #1 closure
→ verified candidate S1
→ fresh-context advisor #2
→ Resolution Batch #2
→ Resolution Transaction #2 closure
→ verified candidate S2
→ complete'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-inputs-92fe04e6635a]
kind = "requires"
slice = "advisor-two-pass-orchestration"
pattern = "Inputs: 親確定の verified candidate S0、fresh-context advisor #1 / #2 の invocation Data、検証済み kernel / role / Resolution Transaction Data、親が loop 開始時に選択・固定した resolution execution bound。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-parent-roundtrip-466bae8affbf]
kind = "requires"
slice = "advisor-two-pass-orchestration"
pattern = "各 advisor output は得た時点で Agentic な親へ返す。親は insight の採否、Resolution Batch の確定、Resolution Transaction の裁定と closure、verify、semantic progress を既存の唯一の正本に従って行い、その結果 Data を次の固定 step へ再投入する。Flow は insight の採否、Transaction 内部、verify または semantic progress の値を決めず、これらの intermediate result を initial Inputs として要求しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.producer-two-pass-no-result-input-33cc50432e68]
kind = "forbids"
slice = "advisor-two-pass-orchestration"
pattern = "Inputs: 親確定の candidate snapshot、read-only advisor の各 insight、Resolution Batch / Transaction の既存 Data、verify と semantic progress の結果。"
applies_to = ["claude", "codex", "cursor"]

[contracts.producer-batch-loader-pointer-e48ae4dde797]
kind = "requires"
slice = "plan-candidate-producer-batch-resolve-kernel-loader"
pattern = "`producer-invocation-preflight` がこの Loader Data を消費する唯一の loader procedure である。この section は列挙 Data の mapping だけを提供し、load / identity / required section / failure routing を再定義しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.producer-batch-loader-no-procedure-4d983aec799b]
kind = "forbids"
slice = "plan-candidate-producer-batch-resolve-kernel-loader"
pattern = "親は上記 Loader Data の field を使って load と必要本文の検証を行い"
applies_to = ["claude", "codex", "cursor"]

[contracts.producer-necessity-loader-pointer-72439dca3f27]
kind = "requires"
slice = "candidate-producer-boundary"
pattern = "necessity-kernel の loader procedure は `producer-invocation-preflight` だけが所有する。この mapping は Loader Data と検証済み本文の injection boundary だけを提供し、load / identity / required section / failure routing を再定義しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.producer-necessity-loader-no-procedure-a90d171cfe09]
kind = "forbids"
slice = "candidate-producer-boundary"
pattern = "candidate Claim を判定する前に、advisor 起動の有無にかかわらず、親は上記 Loader Data の field を使って load と必要本文の検証を行い"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-producer-preflight-invocation-boundary-5722d4a9d94b]
kind = "requires"
slice = "plan-agent-proposal-resolution-ownership"
pattern = "plan-agent は caller mapping、authority injection、resolution execution bound、`producer-invocation-preflight` の invocation boundary、および `plan-artifact-design` の parent-owned load を所有する。kernel / `producer-invocation-preflight` の load / identity / required section / failure routing は owner である plan-candidate-producer に委ね、この Skill では再定義しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-producer-loader-action-forbidden-cc23ade40119]
kind = "forbids"
slice = "plan-agent-proposal-resolution-ownership"
pattern = "batch-resolve-kernel loader を親 Action として一度だけ実行する"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-gate-routing-current-round-input-8ca4e7778de9]
kind = "requires"
slice = "candidate-structural-gate-routing"
pattern = "Inputs: producer status、verified candidate snapshot、gate assessment、`rounds.limit`、親確定の `current round`、gate evidence、producer と gate の親確定 context。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-gate-routing-reversed-limit-forbidden-9cc788d7e7bd]
kind = "forbids"
slice = "candidate-structural-gate-routing"
pattern = "current round が parent-fixed limit 以上でも、gate evidence を入力に producer retry する"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-gate-routing-flow-1776c6dd3167]
kind = "requires"
slice = "candidate-structural-gate-routing"
pattern = "Trigger: 親が producer status / candidate snapshot、gate assessment、独立した structural gate budget を確定したとき。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-dispatch-flow-e7d458c1fbd1]
kind = "requires"
slice = "review-dispatch"
pattern = "Trigger: 親が artifact snapshot と review readiness を確定し、review routing を要求したとき。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-dispatch-flow-routing-e106310fc46b]
kind = "requires"
slice = "review-dispatch"
pattern = "Procedure: parent-confirmed applicability は opt-out より先に評価する。nonapplicable は normal completion、applicable + opt-out は normal completion、applicable + no opt-out + ready は review dispatch とする。readiness failure または reviewer failure は review-not-established として親へ返す。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-dispatch-flow-outcomes-90f2453bdf32]
kind = "requires"
slice = "review-dispatch"
pattern = "Outcomes: nonapplicable、opt-out、review dispatch、または明示的な `review-not-established`。Flow は accept を返さず、finding の意味的な採否は親へ返す。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-dispatch-flow-no-stop-85266337b425]
kind = "forbids"
slice = "review-dispatch"
pattern = "`stop-incomplete`"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-two-pass-duplicate-forbidden-413842fb0d7b]
kind = "forbids"
slice = "plan-agent-proposal-resolution-ownership"
pattern = '''candidate S0
→ fresh-context advisor #1
→ Resolution Batch #1
→ Resolution Transaction #1'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-preflight-before-two-pass-26ae94248770]
kind = "order"
before = "producer-invocation-preflight"
after = "advisor-two-pass-orchestration"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-gate-before-review-d05ca81bd2d2]
kind = "order"
before = "candidate-structural-gate-routing"
after = "review-dispatch"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-flow-section-occurrences]
kind = "occurrences"
pattern = "## Programmatic Flows"
count = 1
paths = [
  "plugins/claude/skills/plan-candidate-producer/SKILL.md",
  "plugins/codex/skills/plan-candidate-producer/SKILL.md",
  "plugins/cursor/skills/plan-candidate-producer/SKILL.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-flow-section-occurrences]
kind = "occurrences"
pattern = "## Programmatic Flows"
count = 1
paths = [
  "plugins/claude/skills/plan-agent/SKILL.md",
  "plugins/codex/skills/plan-agent/SKILL.md",
  "plugins/cursor/skills/plan-agent/SKILL.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-flow-trigger-occurrences]
kind = "occurrences"
pattern = "Trigger:"
count = 2
paths = ["plugins/claude/skills/plan-candidate-producer/SKILL.md", "plugins/codex/skills/plan-candidate-producer/SKILL.md", "plugins/cursor/skills/plan-candidate-producer/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-flow-inputs-occurrences]
kind = "occurrences"
pattern = "Inputs:"
count = 2
paths = ["plugins/claude/skills/plan-candidate-producer/SKILL.md", "plugins/codex/skills/plan-candidate-producer/SKILL.md", "plugins/cursor/skills/plan-candidate-producer/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-flow-procedure-occurrences]
kind = "occurrences"
pattern = "Procedure:"
count = 2
paths = ["plugins/claude/skills/plan-candidate-producer/SKILL.md", "plugins/codex/skills/plan-candidate-producer/SKILL.md", "plugins/cursor/skills/plan-candidate-producer/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-flow-outcomes-occurrences]
kind = "occurrences"
pattern = "Outcomes:"
count = 2
paths = ["plugins/claude/skills/plan-candidate-producer/SKILL.md", "plugins/codex/skills/plan-candidate-producer/SKILL.md", "plugins/cursor/skills/plan-candidate-producer/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-flow-trigger-occurrences]
kind = "occurrences"
pattern = "Trigger:"
count = 3
paths = ["plugins/claude/skills/plan-agent/SKILL.md", "plugins/codex/skills/plan-agent/SKILL.md", "plugins/cursor/skills/plan-agent/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-flow-inputs-occurrences]
kind = "occurrences"
pattern = "Inputs:"
count = 3
paths = ["plugins/claude/skills/plan-agent/SKILL.md", "plugins/codex/skills/plan-agent/SKILL.md", "plugins/cursor/skills/plan-agent/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-flow-procedure-occurrences]
kind = "occurrences"
pattern = "Procedure:"
count = 3
paths = ["plugins/claude/skills/plan-agent/SKILL.md", "plugins/codex/skills/plan-agent/SKILL.md", "plugins/cursor/skills/plan-agent/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-flow-outcomes-occurrences]
kind = "occurrences"
pattern = "Outcomes:"
count = 3
paths = ["plugins/claude/skills/plan-agent/SKILL.md", "plugins/codex/skills/plan-agent/SKILL.md", "plugins/cursor/skills/plan-agent/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-preflight-flow-occurrences]
kind = "occurrences"
pattern = "### producer-invocation-preflight"
count = 1
paths = ["plugins/claude/skills/plan-candidate-producer/SKILL.md", "plugins/codex/skills/plan-candidate-producer/SKILL.md", "plugins/cursor/skills/plan-candidate-producer/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-flow-occurrences]
kind = "occurrences"
pattern = "### advisor-two-pass-orchestration"
count = 1
paths = ["plugins/claude/skills/plan-candidate-producer/SKILL.md", "plugins/codex/skills/plan-candidate-producer/SKILL.md", "plugins/cursor/skills/plan-candidate-producer/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-gate-routing-flow-occurrences]
kind = "occurrences"
pattern = "### candidate-structural-gate-routing"
count = 1
paths = ["plugins/claude/skills/plan-agent/SKILL.md", "plugins/codex/skills/plan-agent/SKILL.md", "plugins/cursor/skills/plan-agent/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-dispatch-flow-occurrences]
kind = "occurrences"
pattern = "### review-dispatch"
count = 1
paths = ["plugins/claude/skills/plan-agent/SKILL.md", "plugins/codex/skills/plan-agent/SKILL.md", "plugins/cursor/skills/plan-agent/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-local-artifact-flow-occurrences]
kind = "occurrences"
pattern = "### local-artifact-completion"
count = 1
paths = ["plugins/claude/skills/plan-agent/SKILL.md", "plugins/codex/skills/plan-agent/SKILL.md", "plugins/cursor/skills/plan-agent/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-local-artifact-flow-62a763a00b81]
kind = "requires"
slice = "plan-agent-local-artifact-completion"
pattern = "Trigger: 親が semantic completion eligibility と、Agentic / mixed side で確定した exact `publication_target` を渡したとき。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-local-artifact-flow-inputs-d42da346f04c]
kind = "requires"
slice = "plan-agent-local-artifact-completion"
pattern = "Inputs: semantic completion eligibility、凍結した成果物本文 bytes、および親確定の `publication_target` Data。`publication_target` は existing destination の observed destination object identity、または OS-temp の verified temp-root identity / top-level `exact_destination` / exclusive creation intent の排他的 Data を持ち、作成前の directory object identity を要求しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-local-artifact-flow-no-future-7af6a4e020e1]
kind = "forbids"
slice = "plan-agent-local-artifact-completion"
pattern = "Inputs: semantic completion eligibility、凍結した成果物本文 bytes、exact destination、exact filename、finite retry bound、qualification evidence、destination object identity、および必要な OS-temp identity / creation intent。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-local-artifact-flow-ff9ec467a47f]
kind = "requires"
slice = "plan-agent-local-artifact-completion"
pattern = "Outcomes: published result と consumer の outward status / stdout projection、`destination-reselection-required`、または `incomplete`。"
applies_to = ["claude", "codex", "cursor"]

# Why Not: file-global field counts can be satisfied by a decoy after the real Flow loses its Procedure.
[contracts.plan-agent-local-artifact-flow-2c04dd8a5208]
kind = "requires"
slice = "plan-agent-local-artifact-completion"
pattern = '''Trigger: 親が semantic completion eligibility と、Agentic / mixed side で確定した exact `publication_target` を渡したとき。
Inputs: semantic completion eligibility、凍結した成果物本文 bytes、および親確定の `publication_target` Data。`publication_target` は existing destination の observed destination object identity、または OS-temp の verified temp-root identity / top-level `exact_destination` / exclusive creation intent の排他的 Data を持ち、作成前の directory object identity を要求しない。
Procedure: skill-relative `../../references/plan-artifact-publication.md` を publication invocation 前に一度だけ load し、identity `plan-artifact-publication-v1` と必要本文を検証する。検証済み reference の `programmatic-publication` Flow に、親確定の `publication_target` をそのまま渡す。Flow の結果を受け取り、published result から `final-candidate` / `incomplete` と stdout の Result、Summary、必要な Human Attention、Artifact path だけを projection する。consumer は target selection、candidate ranking、filename、retry bound、publication procedure を再実行・複製しない。
```text
publication_reference = ../../references/plan-artifact-publication.md
publication_load_timing = once before programmatic-publication use
publication_identity = plan-artifact-publication-v1
publication_use = parent-confirmed publication_target -> programmatic-publication -> outward status/stdout projection
```
Outcomes: published result と consumer の outward status / stdout projection、`destination-reselection-required`、または `incomplete`。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-local-artifact-flow-order]
kind = "order"
before = "plan-agent-artifact-eligibility"
after = "plan-agent-publication-inputs"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-local-artifact-flow-loader-before-outcomes]
kind = "order"
before = "plan-agent-publication-loader"
after = "plan-agent-publication-outcomes"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-programmatic-boundary-09500f9678d9]
kind = "requires"
slice = "plan-agent-programmatic-flows"
pattern = "Flow の procedure、条件、outcome は固定であり、Agent は override、bypass、置換しない。outcome 後に複数の妥当な Action が残る意味判断は Agentic な親へ返す。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-local-artifact-completion-ed6948b7125b]
kind = "requires"
slice = "plan-agent-local-artifact-completion"
pattern = "### local-artifact-completion"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-no-third-pass-13eed53ddec1]
kind = "forbids"
slice = "advisor-two-pass-orchestration"
pattern = "第3 passを起動する"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-gate-routing-flow-procedure-633919de928a]
kind = "requires"
slice = "candidate-structural-gate-routing"
pattern = "Procedure: producer `stop-incomplete`、context-not-established、insufficient-evidence、invalid budget は `stop-incomplete` とする。gate pass は downstream へ進める。gate return は current round が parent-fixed limit 未満の場合だけ、gate evidence を入力に producer retry する。limit 到達の gate return は `stop-incomplete` とする。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-gate-routing-flow-outcomes-68c7abf022a2]
kind = "requires"
slice = "candidate-structural-gate-routing"
pattern = "Outcomes: downstream dispatch、producer retry、または `stop-incomplete`。Flow は producer `stop-incomplete` / insufficient-evidence を plain `incomplete` へ変換せず、round limit の選択・固定、gate assessment の意味、candidate の修正と採否は Flow 外の親へ返す。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-optout-reversal-37ffac39f5a0]
kind = "forbids"
slice = "review-dispatch"
pattern = "opt-out を applicability より先に判定する"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-preflight-field-order]
kind = "order"
before = "producer-preflight-trigger"
after = "producer-preflight-inputs"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-preflight-inputs-before-procedure]
kind = "order"
before = "producer-preflight-inputs"
after = "producer-preflight-procedure"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-preflight-procedure-before-outcomes]
kind = "order"
before = "producer-preflight-procedure"
after = "producer-preflight-outcomes"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-field-order]
kind = "order"
before = "advisor-two-pass-trigger"
after = "advisor-two-pass-inputs"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-inputs-before-procedure]
kind = "order"
before = "advisor-two-pass-inputs"
after = "advisor-two-pass-procedure"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-candidate-producer-two-pass-procedure-before-outcomes]
kind = "order"
before = "advisor-two-pass-procedure"
after = "advisor-two-pass-outcomes"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-gate-field-order]
kind = "order"
before = "gate-routing-trigger"
after = "gate-routing-inputs"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-gate-inputs-before-procedure]
kind = "order"
before = "gate-routing-inputs"
after = "gate-routing-procedure"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-gate-procedure-before-outcomes]
kind = "order"
before = "gate-routing-procedure"
after = "gate-routing-outcomes"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-field-order]
kind = "order"
before = "review-dispatch-trigger"
after = "review-dispatch-inputs"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-inputs-before-procedure]
kind = "order"
before = "review-dispatch-inputs"
after = "review-dispatch-procedure"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-agent-review-procedure-before-outcomes]
kind = "order"
before = "review-dispatch-procedure"
after = "review-dispatch-outcomes"
applies_to = ["claude", "codex", "cursor"]

# Why Not: review-refine keeps reviewer findings and semantic adjudication with the parent; these blocks only route fixed Data.
[contracts.review-refine-programmatic-boundary-794c4fca3145]
kind = "requires"
slice = "review-refine-programmatic-flows"
pattern = "Flow の procedure、条件、outcome は固定であり、Agent は override、bypass、置換しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-reference-preflight-flow-d2e902bf9823]
kind = "requires"
slice = "review-reference-preflight"
pattern = '''Trigger: 親が reviewer result collection 後の parent adjudication、または最初の Resolution Transaction の各 timing に向けて reference preflight を要求したとき。
Inputs: `reality-model-observation-kernel-v1`、`deletion-test-method-v1`、`batch-resolve-kernel-v1` の Loader Data（path、timing、identity、required_sections、failure、owner、delegate_path_resolution）、既存の `判定基準` / `必要な周辺 context`、および role mapping Data。
Procedure: `reality-model-observation-kernel-v1` は通常 reviewer result collection 後、parent adjudication 直前に一度だけ load と identity / 必要本文を検証する。`deletion-test-method-v1` は final-trim reviewer result collection 後、parent adjudication 直前に一度だけ load と identity / 必要本文を検証する。`batch-resolve-kernel-v1` は最初の Resolution Transaction の前に一度だけ load と identity / 必要本文を検証する。RMO / Deletion Test の検証済み本文は既存の `判定基準` または `必要な周辺 context` へ、batch の検証済み本文は既存の role mapping へ注入する。timing と loader を統合せず、RMO / Deletion Test failure は `return-to-parent`、batch failure は既存の意味どおり `stop-incomplete` または `review-not-established` とする。
Outcomes: criteria / role mapping を注入済みの `ready`、`review-not-established`、`stop-incomplete`、または RMO / Deletion Test failure の `return-to-parent`。loader の failure を突破せず、loader 本文の意味判断は Flow 外の親へ返す。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-deferred-freeze-flow-5129b54f4308]
kind = "requires"
slice = "deferred-freeze"
pattern = '''Trigger: 親が finding を `deferred` と裁定済みで、その凍結結果を次 round へ反映するよう要求したとき。
Inputs: 親の `deferred` 裁定、finding ID / evidence / 理由、current `finding_ledger`、`hold_ledger`、次 round の input と snapshot、review context。
Procedure: `deferred` finding を `hold_ledger` に記録し、次 round の input へ台帳を渡し、reviewer へ根拠のない再指摘・深掘りを抑制する。新しい根拠のない再指摘は既存保留へ紐付け、保留事項を根拠に追加仕様や例外処理を派生させない。finding の `deferred` 裁定自体はこの Flow で行わない。
Outcomes: 次 round input と再指摘抑制を含む `deferred-frozen` Data。loop 後の保留事項の扱いと追加 Action は親または人間へ返す。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-induced-loop-eligibility-flow-bf83dc29d0dd]
kind = "requires"
slice = "induced-loop-eligibility"
pattern = '''Trigger: 親が通常 round の因果、severity、件数、直近2回の `plan-adversarial-reviewer` round Data を確定し、induced-loop eligibility の固定計算を要求したとき。
Inputs: 全 finding ledger、親確定の `induced` / `induced_by` 因果 Data、`軽微` / `修正推奨` / `修正必須` の severity、親の裁定、各通常 round の induced / non-induced 件数、直近2回の `plan-adversarial-reviewer` round identity、および final trim / 他 reviewer を除外する識別 Data。
Procedure: 親が `修正推奨` 以上と確定した finding だけを母数に induced count と non-induced count を計算し、induced count が non-induced count を上回る round だけを `induced_dominant` とする。母数が空の round、ちょうど半数、final trim、同じ round の他 reviewer は成立させない。直近2回の通常 round がともに `induced_dominant` で、両 round とも非誘発の `修正必須` が 0 のときだけ `eligible`、それ以外は `not-eligible` とする。旧基準状態を窓の開始位置にせず、因果と severity の意味は再判定しない。
Outcomes: `eligible` または `not-eligible` と計算 evidence。induced / severity の意味、eligible 後の停止選択、親の裁定は Flow 外の親へ返す。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-round-limit-termination-flow-4163060a87d8]
kind = "requires"
slice = "round-limit-termination"
pattern = '''Trigger: 親が `adversarial_review_count` と parent-fixed `rounds.limit` の到達を確定し、打ち切り routing を要求したとき。
Inputs: parent-fixed `rounds.limit`、`adversarial_review_count`、current verified snapshot、finding ledger、未解決 finding 集合。
Procedure: `rounds.limit` 到達時だけ未解決 finding 集合を評価し、空なら `converged`、残存があれば `round-limit` とする。limit 前の continue / converged 判断はこの Flow で行わない。
Outcomes: `converged` または未解決 finding を保持した `round-limit`。round の継続、通常の converged 判断、termination の意味は Flow 外の親へ返す。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-final-trim-count-routing-flow-e6b53d9ad316]
kind = "requires"
slice = "final-trim-count-and-routing"
pattern = '''Trigger: 親が termination、未解決 finding、artifact kind、over-engineering settings を確定し、final trim の routing を要求したとき。
Inputs: `termination`、未解決 finding 集合、`artifact_kind`、`over_engineering_review` の `threshold` / `base_rounds` / `escalated_rounds`、`adversarial_review_count`、および親確定の trim applicability Data。
Procedure: 省略時の settings は `threshold: 5`、`base_rounds: 1`、`escalated_rounds: 3` とし、部分上書きを許す。settings を `threshold >= 0`、`base_rounds >= 1`、`escalated_rounds >= base_rounds` で検証し、不正値は `input-error` とする。`termination` が `converged` または `induced-loop`、未解決 finding が空、かつ artifact が適用対象のプラン系である場合だけ eligible とし、回数を `adversarial_review_count > threshold ? escalated_rounds : base_rounds` で計算する。それ以外は `skip` とする。
Outcomes: trim count を含む `eligible`、`skip`、または settings validation の `input-error`。trim finding の採否、削減内容、rejected への戻し、後続 Action は Flow 外の親へ返す。'''
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-flow-section-occurrences]
kind = "occurrences"
pattern = "## Programmatic Flows"
count = 1
paths = [
  "plugins/claude/skills/review-refine/SKILL.md",
  "plugins/codex/skills/review-refine/SKILL.md",
  "plugins/cursor/skills/review-refine/SKILL.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-flow-trigger-occurrences]
kind = "occurrences"
pattern = "Trigger:"
count = 5
paths = ["plugins/claude/skills/review-refine/SKILL.md", "plugins/codex/skills/review-refine/SKILL.md", "plugins/cursor/skills/review-refine/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-flow-inputs-occurrences]
kind = "occurrences"
pattern = "Inputs:"
count = 5
paths = ["plugins/claude/skills/review-refine/SKILL.md", "plugins/codex/skills/review-refine/SKILL.md", "plugins/cursor/skills/review-refine/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-flow-procedure-occurrences]
kind = "occurrences"
pattern = "Procedure:"
count = 5
paths = ["plugins/claude/skills/review-refine/SKILL.md", "plugins/codex/skills/review-refine/SKILL.md", "plugins/cursor/skills/review-refine/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-flow-outcomes-occurrences]
kind = "occurrences"
pattern = "Outcomes:"
count = 5
paths = ["plugins/claude/skills/review-refine/SKILL.md", "plugins/codex/skills/review-refine/SKILL.md", "plugins/cursor/skills/review-refine/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-reference-preflight-flow-occurrences]
kind = "occurrences"
pattern = "### review-reference-preflight"
count = 1
paths = ["plugins/claude/skills/review-refine/SKILL.md", "plugins/codex/skills/review-refine/SKILL.md", "plugins/cursor/skills/review-refine/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-deferred-freeze-flow-occurrences]
kind = "occurrences"
pattern = "### deferred-freeze"
count = 1
paths = ["plugins/claude/skills/review-refine/SKILL.md", "plugins/codex/skills/review-refine/SKILL.md", "plugins/cursor/skills/review-refine/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-induced-loop-flow-occurrences]
kind = "occurrences"
pattern = "### induced-loop-eligibility"
count = 1
paths = ["plugins/claude/skills/review-refine/SKILL.md", "plugins/codex/skills/review-refine/SKILL.md", "plugins/cursor/skills/review-refine/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-round-limit-flow-occurrences]
kind = "occurrences"
pattern = "### round-limit-termination"
count = 1
paths = ["plugins/claude/skills/review-refine/SKILL.md", "plugins/codex/skills/review-refine/SKILL.md", "plugins/cursor/skills/review-refine/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-final-trim-flow-occurrences]
kind = "occurrences"
pattern = "### final-trim-count-and-routing"
count = 1
paths = ["plugins/claude/skills/review-refine/SKILL.md", "plugins/codex/skills/review-refine/SKILL.md", "plugins/cursor/skills/review-refine/SKILL.md"]
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-reference-preflight-field-order]
kind = "order"
before = "review-reference-preflight-trigger"
after = "review-reference-preflight-inputs"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-reference-preflight-inputs-before-procedure]
kind = "order"
before = "review-reference-preflight-inputs"
after = "review-reference-preflight-procedure"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-reference-preflight-procedure-before-outcomes]
kind = "order"
before = "review-reference-preflight-procedure"
after = "review-reference-preflight-outcomes"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-deferred-freeze-field-order]
kind = "order"
before = "deferred-freeze-trigger"
after = "deferred-freeze-inputs"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-deferred-freeze-inputs-before-procedure]
kind = "order"
before = "deferred-freeze-inputs"
after = "deferred-freeze-procedure"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-deferred-freeze-procedure-before-outcomes]
kind = "order"
before = "deferred-freeze-procedure"
after = "deferred-freeze-outcomes"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-induced-loop-field-order]
kind = "order"
before = "induced-loop-eligibility-trigger"
after = "induced-loop-eligibility-inputs"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-induced-loop-inputs-before-procedure]
kind = "order"
before = "induced-loop-eligibility-inputs"
after = "induced-loop-eligibility-procedure"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-induced-loop-procedure-before-outcomes]
kind = "order"
before = "induced-loop-eligibility-procedure"
after = "induced-loop-eligibility-outcomes"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-round-limit-field-order]
kind = "order"
before = "round-limit-termination-trigger"
after = "round-limit-termination-inputs"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-round-limit-inputs-before-procedure]
kind = "order"
before = "round-limit-termination-inputs"
after = "round-limit-termination-procedure"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-round-limit-procedure-before-outcomes]
kind = "order"
before = "round-limit-termination-procedure"
after = "round-limit-termination-outcomes"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-final-trim-field-order]
kind = "order"
before = "final-trim-count-and-routing-trigger"
after = "final-trim-count-and-routing-inputs"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-final-trim-inputs-before-procedure]
kind = "order"
before = "final-trim-count-and-routing-inputs"
after = "final-trim-count-and-routing-procedure"
applies_to = ["claude", "codex", "cursor"]

[contracts.review-refine-final-trim-procedure-before-outcomes]
kind = "order"
before = "final-trim-count-and-routing-procedure"
after = "final-trim-count-and-routing-outcomes"
applies_to = ["claude", "codex", "cursor"]
