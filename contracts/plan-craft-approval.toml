[contracts.proposal-dialogue-parent-requires-0655da896300]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "`plan-craft-approval` の同じ親 context 内だけ"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-producer-boundary-requires-8cdd3a9238b2]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "`proposal` と同じ candidate producer として、後段工程を選択・起動せず caller-owned parent へ返す。"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-decisions-requires-7b475f84ef04]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "`採用` / `却下` / `保留` / `修正して採用`"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-recommendation-requires-fc587313eaf3]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "親の推奨を人間の回答として扱わない"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-no-answer-requires-b79c50a00736]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "無回答・曖昧な反応を承認として扱わず"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-sequential-snapshot-requires-70f6381f565e]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "採用分だけを working snapshot へ逐次反映して verification し、複数の判断を一括反映しない。"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-freeze-requires-41ed8038cc5f]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "direction freeze 候補は、採用提案が反映・verification 済み、主要判断が明示裁定済み、保留事項が scope 外へ分離済み、\nblocking な人間判断がなく、ユーザーが現在の方向を確認済みの場合だけ成立する。"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-raw-ledger-requires-64c6d23487b1]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "反映 snapshot、verification を decision ledger として会話内 Data に保持するが、YAML、内部 schema、raw ledger は\nユーザーへ提示しない。"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-stop-requires-5d6871257e15]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "`stop-incomplete`"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-ledgers-requires-d68cd2f350af]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "decision ledger と adoption ledger は分離する"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-insight-question-requires-fef19f44e2e5]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "insight から質問を自動生成しない"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-advisor-phase-requires-171ecf2fc3d6]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "advisor を固定 phase にせず"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-empty-insight-requires-30f4ef1af0eb]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "`insights: []` を"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-advisor-requires-d7887b272940]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "plan-quality-advisor"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-criteria-requires-87ccbedaccba]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "判定基準"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-context-requires-3037baa340d6]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "必要な周辺 context"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-identity-requires-cde3b4178b8a]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "identity"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-task-spec-requires-d698eed31f52]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "Task Specification"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-missing-requires-7dbb3c85035f]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "不足"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-reference-requires-c4c7280be860]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "../../references/necessity-kernel.md"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-parent-action-requires-d05d2eedbe26]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "advisor 起動の有無にかかわらず、親は生成後の skill directory から package-root reference へ skill-relative `../../references/necessity-kernel.md` を読み"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-advisor-data-requires-0ca25afaa214]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "`plan-quality-advisor` 起動時は既取得 Data を既存の判定基準として渡す。"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-failure-requires-0315317be7e7]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "reference の不足、identity 不一致、\n読み取り失敗があれば推測せず `stop-incomplete` へ返し"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-vocabulary-requires-e13a3288d498]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "既存語彙へ写像"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-no-verdict-requires-685350cabecd]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "新verdict fieldではない"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-budget-requires-a49ca7e244dc]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "round budget"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-termination-requires-11f2f125d53f]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "termination"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-no-link-requires-59f2ef7d2acf]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "直結させない"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-gate-requires-c667784b1acb]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "`structural-health-gate` の意味は この mapping の対象外である"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-baseline-forbids-ea5775f199d5]
kind = "forbids"
slice = "proposal-dialogue-boundary"
pattern = "baseline_round"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-saturation-forbids-538fcd94205d]
kind = "forbids"
slice = "proposal-dialogue-boundary"
pattern = "飽和窓"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-review-loop-forbids-635c70de2304]
kind = "forbids"
slice = "proposal-dialogue-boundary"
pattern = "review-loop"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-responsibility-requires-b01d31f5587e]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "人間は結果責任を、planner は evidence に基づく経過責任を担う。"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-no-research-delegation-requires-04a1ef6f157c]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "Agent で解消可能な曖昧さを人間へ返さない。"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-density-requires-b06fdf3ef4bc]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "`A` / `B` / `C` は mode、enum、永続 state ではなく、判断点ごとの対話密度である。"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-freeze-meaning-requires-291f374b875e]
kind = "requires"
slice = "proposal-dialogue-boundary"
pattern = "direction freeze は成果物全文ではなく"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-mode-state-forbids-1edf5c5386d1]
kind = "forbids"
slice = "proposal-dialogue-boundary"
pattern = "`dialogue_mode`"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-advisor-direct-forbids-cb8dcd75e095]
kind = "forbids"
slice = "proposal-dialogue-boundary"
pattern = "advisor insight をそのまま人間へ提示する"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-order-requires-f8347c3031bb]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "proposal-dialogue → structural-health-gate → review-loop"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-freeze-requires-22d39a73091d]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "direction freeze"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-applicability-first-requires-606808497aff]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "review 省略の明示より reviewer 適用可否の判定を先に行う"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-not-applicable-requires-8b1a6a39d863]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "既定 reviewer の適用対象外なら、review goal に対応する別 reviewer の有無にかかわらず `review-loop` に投入せず、通常の起草確定へ進む。"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-fixed-review-requires-b01feb2f4c96]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "reviewer 適用可能な成果物は、ユーザーによる review の明示要求がなくても固定工程として `review-loop` へ渡す。"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-explicit-skip-requires-7127b7a6cb00]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "ユーザーが review 省略を明示した場合は、確定候補とせず、review 未実施の起草物と残存 risk を添えて未完了として返す。"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-converged-requires-d4d4954b86aa]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "`converged`"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-induced-clean-requires-f1d8351c69e7]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "未解決 finding のない `induced-loop`"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-review-failure-requires-bb625372fddd]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "レビュー不成立"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-round-limit-requires-b1875bbb3a81]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "`round-limit`"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-stop-requires-bf0cad0ca037]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "`stop-incomplete`"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-induced-unresolved-requires-a7a6f2b6823d]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "未解決 finding を伴う `induced-loop`"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-no-alternative-requires-3a022e0ae565]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "代替 evidence で完了扱いにしない"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-no-reverse-requires-02e2dbbac64d]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "自動逆遷移しない"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-human-confirmation-requires-28d1af738b7c]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "decision ledger で人間が裁定済みの方向性を変更・撤回する finding は、局所修正で閉じる場合も親だけで採用せず\n`人間確認` へ裁定する。人間の再判断後だけ成果物へ反映し、既存の裁定区分を増やさない。"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-same-content-requires-b51fa9321183]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "同一内容へ"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-caller-context-requires-8953f4a646ab]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "generic `caller_context` Data"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-review-goal-requires-e41dd8f5e108]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "`review_goal` は、ユーザー指定の review goal や追加の具体的な risk がない場合、「実装前プランの具体的な failure path を確認し、確定候補にできるか判断する」とする。"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-review-goal-requires-37f40fb81264]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "これは plan review 自体の既定目的であり、毎回 risk を事前発見することを要求しない。"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-review-goal-requires-acf2d1b26e75]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "ユーザー指定 goal や追加 risk は既存 reviewer の責務内で追加できる。"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-review-goal-forbids-bc111c83c13c]
kind = "forbids"
slice = "plan-craft-approval-handoff"
pattern = "追加 risk がなければ `review_goal` を設定しない"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-fallback-forbids-c8fe8122f54a]
kind = "forbids"
slice = "plan-craft-approval-handoff"
pattern = "ただし goal に対応する別 reviewer があれば `review-loop` へ渡す"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-final-acceptance-requires-0aa1c8018ae3]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "final acceptance は direction freeze と分離し、既定で必須"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-semantic-delta-requires-a95b222bd59e]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "Semantic Delta"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-verification-delta-requires-526d5e93a188]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "Verification Delta"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-replan-requires-e1c83193e6a2]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "大きな purpose または scope の変更"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-freeze-as-final-forbids-29d8e8a66ff5]
kind = "forbids"
slice = "plan-craft-approval-handoff"
pattern = "direction freeze を final acceptance とみなす"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-optional-final-forbids-aec3ccb2d647]
kind = "forbids"
slice = "plan-craft-approval-handoff"
pattern = "final acceptance は任意"
applies_to = ["claude", "codex"]

[contracts.proposal-dialogue-kernel-reference-before-criteria]
kind = "order"
before = "proposal-dialogue-kernel-reference"
after = "proposal-dialogue-kernel-criteria"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-dialogue-before-gate]
kind = "order"
before = "plan-craft-approval-proposal-dialogue-start"
after = "plan-craft-approval-structural-health-gate-start"
applies_to = ["claude", "codex"]

[contracts.plan-craft-approval-gate-before-review]
kind = "order"
before = "plan-craft-approval-structural-health-gate-start"
after = "plan-craft-approval-review-loop-handoff"
applies_to = ["claude", "codex"]
