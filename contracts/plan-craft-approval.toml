[contracts.clarify-caller-data-fab7a72d8afa]
kind = "requires"
slice = "plan-craft-approval-clarify-caller"
pattern = '''application = public clarify-it in the same plan-craft-approval parent context
dialogue_norm = inherit clarify-it Specification, Method, and Casebook without copying or override
caller_owns = invocation boundary | binding Human authority | verified workflow Data | direction freeze | downstream workflow
public_extension = none'''
applies_to = ["claude", "codex", "cursor"]

[contracts.execution-bound-data-8e7bc9625afb]
kind = "requires"
slice = "plan-craft-approval-execution-bound"
pattern = '''fix_timing = at each clarify or resolution loop start
immutability = unchanged during that loop
terminal = bound reached or no semantic progress with material decisions remaining -> preserve remaining decisions and reasons -> plan-craft-approval incomplete -> prohibit downstream
public_contract = no fixed value, public parameter, schema, or resolve_rounds
budget_boundary = separate from structural-health-gate rounds and review limits
clarify_stopped = never fabricate this caller-owned terminal as clarify-it Stopped'''
applies_to = ["claude", "codex", "cursor"]

[contracts.kernel-loader-data-81050f00412b]
kind = "requires"
slice = "plan-craft-approval-kernel-mapping"
pattern = '''resolve_path = ../../references/resolve-kernel.md
resolve_load_timing = once before clarify-it application
resolve_identity = resolve-kernel-v1
resolve_dependencies = none
resolve_required_sections = [Caller boundary と role, Current verified snapshot、working state、frontier, Atomic resolution unit, Exit と停止, Kernel non-dependency]
necessity_path = ../../references/necessity-kernel.md
necessity_load_timing = once before candidate Claim adjudication
necessity_identity = necessity-kernel-v1
necessity_dependencies = none
necessity_required_sections = [適用範囲, Task Specification, Claim と evidence, Deletion Test]
failure = incomplete before clarify-it application
owner = plan-craft-approval parent
delegate_path_resolution = false'''
applies_to = ["claude", "codex", "cursor"]

[contracts.kernel-role-data-cf15e934ff45]
kind = "requires"
slice = "plan-craft-approval-kernel-mapping"
pattern = '''caller = plan-craft-approval
resolver = planner
counterpart = human
authority = binding
ledger = decision ledger'''
applies_to = ["claude", "codex", "cursor"]

[contracts.observation-reapply-data-1b1a9e358271]
kind = "requires"
slice = "plan-craft-approval-observation-reapply"
pattern = '''clarify_action_boundary = inherit clarify-it prohibition without override
parent_observation = observe request, repository, Issue, existing specification, and required runtime behavior
observation_data = freeze observation with provenance and evidence while inheriting clarify-it Data distinctions
technical_evidence_gap = return to parent boundary; never Human Decision Point or clarify-it Stopped
parent_action = perform only the minimum required observation
data_update = add the observed Data to the same current decision model
reapply = apply clarify-it to that same model in the same parent context
public_extension = no new status, fixed output schema, or separate context
contradictory_action_permission = prohibited'''
applies_to = ["claude", "codex", "cursor"]

[contracts.observation-action-permission-76976ad013b2]
kind = "forbids"
slice = "plan-craft-approval-observation-reapply"
pattern = '''contradictory_action_permission = allowed'''
applies_to = ["claude", "codex", "cursor"]

[contracts.clarify-projection-data-781df0c3a3a7]
kind = "requires"
slice = "plan-craft-approval-clarify-projection"
pattern = '''Completed = verified Data comparison -> unique direction freeze candidate projection
projection = add no new meaning, condition, or decision
freeze_only_reconfirmation = prohibited
Stopped = preserve reason and remaining decisions -> plan-craft-approval incomplete -> prohibit downstream
Completed_meaning = not workflow completion or candidate acceptance'''
applies_to = ["claude", "codex", "cursor"]

[contracts.projection-reconfirmation-450555d2c5d6]
kind = "forbids"
slice = "plan-craft-approval-clarify-projection"
pattern = '''freeze_only_reconfirmation = required'''
applies_to = ["claude", "codex", "cursor"]

[contracts.reopen-routing-data-117e934a0f70]
kind = "requires"
slice = "plan-craft-approval-reopen-routing"
pattern = '''workflow_order = clarify-it -> direction freeze projection -> optional proposal refinement / freeze-integrity -> structural-health-gate -> review-loop
gate_return = problem / impact / recommendation -> affected dependency closure -> clarify-it application -> new freeze -> new selection -> gate
frozen_review_finding = parent human-confirmation adjudication -> affected dependency closure -> clarify-it application -> new freeze -> new selection -> gate -> review changed closure only
review_stop_incomplete = no automatic clarify-it application; Human explicitly selects resume | incomplete end | out-of-scope split
final_acceptance_correction = affected dependency closure -> clarify-it application -> new freeze -> new selection -> gate -> review changed closure only -> final acceptance
large_purpose_or_scope_change = reformulate the whole public workflow; no local reopen
closure_failure = incomplete; never guess affected scope'''
applies_to = ["claude", "codex", "cursor"]

[contracts.necessity-parent-data-9ede9b1cd4e8]
kind = "requires"
slice = "plan-craft-approval-necessity-parent"
pattern = '''criteria_injection = verified Task Specification + Deletion Test
mapping = necessary -> adopted; unnecessary -> rejected; indeterminate -> unresolved
mapping_target = existing adoption ledger; no new result field'''
applies_to = ["claude", "codex", "cursor"]

[contracts.advisor-human-boundary-6e4a30872e5e]
kind = "requires"
slice = "plan-craft-approval-advisor-parent"
pattern = '''automatic_adoption = prohibited
automatic_question_generation = prohibited
direct_human_question = prohibited
human_direction_impact = unresolved -> parent verifies evidence and reconstructs its own recommendation -> clarify-it application
raw_insight = parent-only unless Human explicitly requests process details'''
applies_to = ["claude", "codex", "cursor"]

[contracts.check1-recovery-data-220cf5108857]
kind = "requires"
slice = "plan-craft-approval-check1-recovery"
pattern = '''check #1 violated | indeterminate = retain all evidence -> affected dependency closure -> local clarify-it application -> verified recovery freeze -> fresh bounded proposal fixed 2 pass -> fresh-context check #2
direct_structural_health_gate = prohibited
legacy_dialogue_route = prohibited
check #2 non-intact = Trust failure incomplete; no reopen'''
applies_to = ["claude", "codex", "cursor"]

[contracts.check1-direct-gate-2e994d259a00]
kind = "forbids"
slice = "plan-craft-approval-check1-recovery"
pattern = '''direct_structural_health_gate = allowed'''
applies_to = ["claude", "codex", "cursor"]

[contracts.check1-legacy-route-6ea33edd1a47]
kind = "forbids"
slice = "plan-craft-approval-check1-recovery"
pattern = '''legacy_dialogue_route = allowed'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-kernel-before-clarify]
kind = "order"
before = "plan-craft-approval-kernel-load"
after = "plan-craft-approval-clarify-start"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-clarify-before-gate]
kind = "order"
before = "plan-craft-approval-clarify-start"
after = "plan-craft-approval-structural-health-gate-start"
applies_to = ["claude", "codex", "cursor"]

[contracts.refinement-selection-requires-f58555dece96]
kind = "requires"
slice = "plan-craft-approval-refinement-selection"
pattern = '''selection_presentation = direction freeze summary same turn; recommend execute or skip + short reason + Yes / No; exactly once
No = skip proposal and freeze-integrity check -> structural-health-gate
Yes = verified direction-freeze candidate S0 -> constrained proposal fixed 2 pass -> fresh-context freeze-integrity check'''
applies_to = ["claude", "codex", "cursor"]

[contracts.refinement-selection-no-run-forbids-caac5c26e0c3]
kind = "forbids"
slice = "plan-craft-approval-refinement-selection"
pattern = "No = run proposal or freeze-integrity check"
applies_to = ["claude", "codex", "cursor"]

[contracts.refinement-selection-yes-skip-forbids-56a421c2bf64]
kind = "forbids"
slice = "plan-craft-approval-refinement-selection"
pattern = "Yes = skip proposal or freeze-integrity check"
applies_to = ["claude", "codex", "cursor"]

[contracts.refinement-stop-requires-d625986ecb2d]
kind = "requires"
slice = "plan-craft-approval-refinement-stop"
pattern = '''proposal stop-incomplete
authority_conflict + verified result = use only returned verified candidate for local Human Decision recovery
otherwise = outward incomplete; no pre-refinement snapshot fallback; no structural-health-gate'''
applies_to = ["claude", "codex", "cursor"]

[contracts.refinement-stop-fallback-forbids-adc7b41012e4]
kind = "forbids"
slice = "plan-craft-approval-refinement-stop"
pattern = "otherwise = pre-refinement snapshot fallback -> structural-health-gate"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-quality-advisor-invocations-requires-78e0d7f8cbbc]
kind = "requires"
slice = "plan-quality-advisor-boundary"
pattern = '''verdict = intact | violated | indeterminate
evidence = authority constraint, refined candidate location, semantic delta or indeterminate cause
binding = parent cannot override
comparison = all authority_constraints independently
normal_output = insight_fields only; non-binding; exclude freeze-integrity verdict/evidence
freeze_integrity_output = verdict/evidence only; binding; exclude normal insight'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-freeze-raw-source-requires-ad816f364b11]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "判断点にならなかった raw specification を含む全意味単位"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-freeze-bijection-requires-8efeae5ee655]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "source ID 集合と constraint ID 集合、1対1、未投影・重複・余分の不在"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-always-check-requires-023b48cfd11f]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "successful refinement 後は semantic change がなくても"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-recovery-scope-requires-dbaa291ad2e8]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = '''affected_scope = constraint IDs + candidate locations -> dependency closure -> affected obligations
request.scope = affected obligations
request.exclude = all other candidate obligations
scope_outside = rejected | out-of-scope; never apply
scope_failure = unresolved locator / closure / exclusivity -> stop-incomplete before proposal'''
applies_to = ["claude", "codex", "cursor"]

[contracts.authority-recovery-fresh-proposal-requires-6cec3c2a2840]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "authority conflict への Human 再判断を反映・verify した後は、最新 verified snapshot と更新済みの完全な constraints で fresh proposal fixed 2 pass"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-trust-stop-requires-dd122fdda6e1]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "check #2 が非 `intact` なら Trust failure の `stop-incomplete` とし、再 reopen しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-trust-report-requires-936768e839eb]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "2回目も `intact` でなかったこと、verdict、問題が残る constraint 周辺、`stop-incomplete` だけを報告し、raw advisor history は露出しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-unresolved-no-gate-requires-b50718e54183]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "未解決 Human Decision が残る candidate は gate へ渡さない"
applies_to = ["claude", "codex", "cursor"]

[contracts.gate-return-compression-requires-1f31f39a5a94]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "gate の問題・影響・推奨対応を Human へ圧縮"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-no-optin-inheritance-requires-17049cc2c14a]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "前 cycle の opt-in や governing value を継承しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.final-reselection-requires-a9c816d8d0ef]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "新しい direction freeze で proposal refinement の `Yes / No` を再確認し、前回の opt-in を継承せず"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-latest-freeze-delta-requires-7e00b730e5a8]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "`Semantic Delta` baseline は final candidate に対応する最新 direction freeze"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-refinement-provenance-requires-838303e99fe8]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "`proposal refinement: executed | skipped` だけを表示する"
applies_to = ["claude", "codex", "cursor"]

[contracts.refinement-provenance-sequences-requires-4557709b92e6]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = '''executed -> gate return -> skipped = skipped
skipped -> gate return -> executed = executed
executed -> Trust recovery -> intact = executed
executed -> final acceptance correction -> skipped = skipped'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-applicability-first-requires-61d3b00cab8a]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "review 省略の明示より reviewer 適用可否の判定を先に行う"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-not-applicable-requires-957326ca3f3a]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "既定 reviewer の適用対象外なら、review goal に対応する別 reviewer の有無にかかわらず `review-loop` に投入せず、通常の起草確定へ進む。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-fixed-review-requires-cf392563aa5f]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "reviewer 適用可能な成果物は、ユーザーによる review の明示要求がなくても固定工程として `review-loop` へ渡す。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-explicit-skip-requires-f34a1e8c0063]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "ユーザーが review 省略を明示した場合は、確定候補とせず、review 未実施の起草物と残存 risk を添えて未完了として返す。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-converged-requires-568f0b08d13a]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "`converged`"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-induced-clean-requires-57549e1a01b8]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "未解決 finding のない `induced-loop`"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-review-failure-requires-9b72ab4ea29e]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "レビュー不成立"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-round-limit-requires-e01366632041]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "`round-limit`"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-stop-requires-4bbc67baa0ab]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "`stop-incomplete`"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-induced-unresolved-requires-d172530f123b]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "未解決 finding を伴う `induced-loop`"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-no-alternative-requires-0f31049e7a4a]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "代替 evidence で完了扱いにしない"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-no-reverse-requires-e8d11b39dddf]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "自動逆遷移しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-same-content-requires-7b7535bc8d88]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "同一内容へ"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-caller-context-requires-bcf97400c66e]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "generic `caller_context` Data"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-review-goal-requires-a63ea137e2eb]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "`review_goal` は、ユーザー指定の review goal や追加の具体的な risk がない場合、「実装前プランの具体的な failure path を確認し、確定候補にできるか判断する」とする。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-review-goal-requires-4966dff9d50f]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "これは plan review 自体の既定目的であり、毎回 risk を事前発見することを要求しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-review-goal-requires-2c76ead9f65a]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "ユーザー指定 goal や追加 risk は既存 reviewer の責務内で追加できる。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-review-goal-forbids-725acaae079f]
kind = "forbids"
slice = "plan-craft-approval-handoff"
pattern = "追加 risk がなければ `review_goal` を設定しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-fallback-forbids-4c9929081ee1]
kind = "forbids"
slice = "plan-craft-approval-handoff"
pattern = "ただし goal に対応する別 reviewer があれば `review-loop` へ渡す"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-final-acceptance-requires-85acc5627ad9]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "final acceptance は direction freeze と分離し、既定で必須とする。人間が明示的に opt-out した場合だけ承認 Action を\n省略できるが、final report は省略しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-review-change-boundary-requires-c8f4ef268682]
kind = "requires"
slice = "plan-craft-approval-handoff"
pattern = "review は frozen decisions を守る限り、実装の具体化、verification の補強、複雑性の削減を行える。frozen decision の\n変更が必要なら、改善案を採用せず `人間確認` へ止める。"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-freeze-as-final-forbids-4002b75d89a7]
kind = "forbids"
slice = "plan-craft-approval-handoff"
pattern = "direction freeze を final acceptance とみなす"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-optional-final-forbids-a31ba6ac5ce3]
kind = "forbids"
slice = "plan-craft-approval-handoff"
pattern = "final acceptance は任意"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-freeze-as-final-variant-forbids-48564f79637a]
kind = "forbids"
slice = "plan-craft-approval-handoff"
pattern = "direction freeze を最終承認として扱う"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-optional-final-variant-forbids-9fe4aa2d50f0]
kind = "forbids"
slice = "plan-craft-approval-handoff"
pattern = "final acceptance は省略可能"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-gate-before-review]
kind = "order"
before = "plan-craft-approval-structural-health-gate-start"
after = "plan-craft-approval-review-loop-handoff"
applies_to = ["claude", "codex", "cursor"]

# Why Not: acceptance timing, artifact purity, path qualification, save commit, output, and authority form one relation.
[contracts.plan-craft-approval-artifact-completion-data-97cdd8bcad6a]
kind = "requires"
slice = "plan-craft-approval-artifact-completion"
pattern = '''workflow = plan-craft-approval
artifact_eligibility = final acceptance completed and verified save success
pre_acceptance_artifact = none at direction freeze, gate, review, or acceptance candidate
artifact_body = frozen final accepted candidate body only
artifact_excludes = [Semantic Delta, Verification Delta, Human Attention, gate result, review result, decision ledger, finding ledger, process history]
incomplete_artifact = none
explicit_destination = highest priority; unsafe or unusable means incomplete without fallback
unspecified_project_local = existing temporary or local-only purpose is directly evidenced; exact final path is ignored and index-unregistered
os_temp_fallback = no candidate, tied candidates, non-Git project, or unconfirmed qualification
project_mutation = do not create a project-local directory or ignore rule
filename = content-derived safe single component
collision = never overwrite an existing file; finite numeric suffix retry only on collision
save = same-filesystem run-owned staging write -> close -> byte-identical readback -> complete-only no-clobber publish
save_failure = incomplete; clean exact staging residue or report it as residual risk
stdout = Result, short Summary, optional Human Attention, Artifact local path
stdout_excludes = full artifact body, Semantic Delta, Verification Delta, gate or review result, decision or finding ledger, process history
summary_opt_out = Artifact only
authority = not Git management, durable persistence, final acceptance, or downstream Action permission'''
applies_to = ["claude", "codex", "cursor"]

# Why Not: staging ownership and OS-temp qualification must stay one security relation across the whole save lifecycle.
[contracts.plan-craft-approval-artifact-security-data-f612737da7b6]
kind = "requires"
slice = "plan-craft-approval-artifact-completion"
pattern = '''staging_creation = atomic exclusive non-follow new object beneath verified destination identity
staging_identity = same object identity through write, readback, and cleanup
qualification_recheck = immediately before staging creation and immediately before publish
destination_reselection = owned staging identity must match and cleanup must succeed first
cleanup_excludes = pre-existing or identity-mismatched object
unsafe_primitive = stop before write or return safe incomplete
os_temp = validated platform temp root -> secure exclusive run-owned directory
os_temp_paths = canonical containment and non-follow for final and staging
os_temp_repo_overlap = project-local qualification applies when canonical path is inside repository
os_temp_unconfirmed = stop before write as incomplete
run_owned_directory_identity = record at creation
run_owned_directory_cleanup = before publish commit on incomplete or destination reselection; after owned staging handling; same identity and empty; non-recursive non-follow remove only
run_owned_directory_residue = identity mismatch, non-empty, or removal failure means leave and report exact residue
run_owned_directory_commit = preserve directory containing published final artifact'''
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-no-retired-autosave]
kind = "forbids"
pattern = "workflow は成果物を自律的に保存しない"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-acceptance-before-stage]
kind = "order"
before = "plan-craft-approval-final-acceptance-complete"
after = "plan-craft-approval-artifact-stage"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-stage-before-publish]
kind = "order"
before = "plan-craft-approval-artifact-stage"
after = "plan-craft-approval-artifact-publish"
applies_to = ["claude", "codex", "cursor"]

[contracts.plan-craft-approval-publish-before-output]
kind = "order"
before = "plan-craft-approval-artifact-publish"
after = "plan-craft-approval-artifact-output"
applies_to = ["claude", "codex", "cursor"]
[contracts.advisor-parent-data-bf74eb13d79a]
kind = "requires"
slice = "plan-craft-approval-advisor-parent"
pattern = '''trigger_only = scope or responsibility boundary change | adopted-Claim dependency | decision-completing additional Claim | non-trivial pre-freeze change chain
advisor = read-only plan-quality-advisor
input = candidate snapshot + verified criteria Data
insight = non-binding Data
adjudicator = plan-craft-approval parent against primary evidence and request
mapping = adopted | rejected | unresolved in adoption ledger
ledger_boundary = decision ledger is separate from adoption ledger
fixed_phase = prohibited
empty_insights = not direction freeze evidence'''
applies_to = ["claude", "codex", "cursor"]

[contracts.advisor-auto-adopt-1a9de706db02]
kind = "forbids"
slice = "plan-craft-approval-advisor-parent"
pattern = "insight を自動採用する"
applies_to = ["claude", "codex", "cursor"]

[contracts.advisor-auto-question-5e256c791978]
kind = "forbids"
slice = "plan-craft-approval-advisor-parent"
pattern = "insight から質問を自動生成する"
applies_to = ["claude", "codex", "cursor"]

[contracts.advisor-fixed-phase-3a74fd9f8de1]
kind = "forbids"
slice = "plan-craft-approval-advisor-parent"
pattern = "advisor を固定 phase とする"
applies_to = ["claude", "codex", "cursor"]

[contracts.advisor-empty-freeze-03948263ae3f]
kind = "forbids"
slice = "plan-craft-approval-advisor-parent"
pattern = "`insights: []` を direction freeze の根拠にする"
applies_to = ["claude", "codex", "cursor"]

[contracts.verification-parent-data-8a7b5297264b]
kind = "requires"
slice = "plan-craft-approval-verification-derivation"
pattern = '''timing = before direction freeze and after each updated snapshot
owner = plan-craft-approval parent Calculation
input = Task Specification + verified workflow Data
coverage = normal | boundary | failure path | side effect | prohibition | responsibility boundary | scope exclude
output = how each obligation will be observed after implementation
clarify_it = do not add this derivation or its schema to clarify-it
blocking_gap = incomplete before direction freeze'''
applies_to = ["claude", "codex", "cursor"]
