[contracts.impl-default-exact-requires-91ec20f8d7c6]
kind = "requires"
slice = "impl-run-owned-default"
pattern = "### Default run-owned checkout\n\nユーザーが既存 checkout、別の isolation/worktree、または worktree を使わない制約を指定していない場合、親は\n`base_snapshot` を確定し、保護する dirty/untracked を記録した後、最初の書き込み Action より前に、その snapshot から\nrun-owned worktree を一つ作成する。作成 Action が run の最初の書き込みであり、source、test、generator、formatter、integration\nを既存の current checkout で先に実行してはならない。run-owned worktree は run 全体の既定 checkout とする。Work Unit 数だけでは追加 worktree を作らない。\n既定実行順は直列のまま、並列 writer や immutable review target など具体的な必要がある場合だけ、既存の\nsafe-parallel 条件に従って追加 isolation を選ぶ。\n\nこの既定の作成では、execution data に `base`（`base_snapshot`）、`owner`（run が所有する resource）、`single_writer`（その時点の\n親または委譲 worker）、`paths`（worktree の絶対 path を含む list）、`integration`（親 QA、review、final writing gate、統合、rollback\nの責任）、`cleanup`（未統合成果、evidence、再開可能性、user constraint を確認してから決める条件）を確定する。worktree が存在する\nこと自体は quality の evidence または accept の根拠にせず、既存の parent QA、review、final writing gate、integration、rollback を\n省略しない。\n\nユーザーが指定した既存 checkout、別 isolation/worktree、または不使用の制約は execution constraint として既定より優先する。\nその指定と品質下限が衝突する場合、無断で run-owned または別経路へ変更せず、確認を求めるか `stop-incomplete` とする。run-owned\nworktree を作成できない場合も current checkout へ暗黙 fallback しない。未完了範囲と evidence を付けて `stop-incomplete` とする。\n作成のために既存の dirty/untracked を commit、move、stash、discard しない。run-owned resource は親が所有し、cleanup は\nrun の accept 成否だけで機械的に削除せず、user-owned resource（ユーザー指定の checkout/worktree や branch）を無断変更・削除しない。"
applies_to = ["claude", "codex"]

[contracts.impl-run-owned-default-requires-24af2bb38936]
kind = "requires"
slice = "impl-run-owned-default"
pattern = "### Default run-owned checkout"
applies_to = ["claude", "codex"]

[contracts.impl-run-owned-default-occurrences]
kind = "occurrences"
pattern = "### Default run-owned checkout"
count = 1
paths = [
  "plugins/claude/skills/impl-lead/references/run-owned-lifecycle.md",
  "plugins/codex/skills/impl-lead/references/run-owned-lifecycle.md",
]
applies_to = ["claude", "codex"]

[contracts.impl-run-owned-default-before-route]
kind = "order"
before = "impl-run-owned-default-start"
after = "impl-route-execution-start"
applies_to = ["claude", "codex"]

[contracts.impl-run-owned-lifecycle-loader-requires-11ecfc78e5d5]
kind = "requires"
slice = "impl-run-owned-lifecycle-loader"
pattern = "この経路を選ぶ場合、親は作成 Action より前に skill-relative `references/run-owned-lifecycle.md` の全文を読み、identity\n`# impl-lead run-owned lifecycle v1`、`Creation`、`Closeout` の各 section と必要本文を確認する。不足、identity 不一致、必要本文の\n欠落、読み取り失敗では推測せず `stop-incomplete` とする。reference は run-owned resource の作成、integration、cleanup の詳細を\n定義し、親が ownership、判断、Action、結果照合を保持する。"
applies_to = ["claude", "codex"]

[contracts.impl-run-owned-lifecycle-reference-identity]
kind = "occurrences"
pattern = "# impl-lead run-owned lifecycle v1"
count = 1
paths = [
  "plugins/claude/skills/impl-lead/references/run-owned-lifecycle.md",
  "plugins/codex/skills/impl-lead/references/run-owned-lifecycle.md",
]
applies_to = ["claude", "codex"]

[contracts.impl-final-writing-loader-requires-19336586c08e]
kind = "requires"
slice = "impl-final-writing-loader"
pattern = "gate の開始前に、親は skill-relative `references/final-writing-gate.md` の全文を読み、identity\n`# impl-lead final writing gate v1`、`Final writing acceptance gate`、`Final writing findings and remediation` の各 section と\n必要本文を確認する。不足、identity 不一致、必要本文の欠落、読み取り失敗では推測せず `stop-incomplete` とする。reference は\nsnapshot、handoff、read-only isolation、finding の裁定、局所 remediation、再 gate、closeout data を定義し、reviewer を writer または\n受入決定者にしない。親が finding の採否、修正経路、final verification、run の受入判断を保持する。"
applies_to = ["claude", "codex"]

[contracts.impl-final-writing-reference-identity]
kind = "occurrences"
pattern = "# impl-lead final writing gate v1"
count = 1
paths = [
  "plugins/claude/skills/impl-lead/references/final-writing-gate.md",
  "plugins/codex/skills/impl-lead/references/final-writing-gate.md",
]
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-dfe4f75786cc]
kind = "requires"
slice = "final-writing-remediation"
pattern = "final writing gate"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-71697deb87bc]
kind = "requires"
slice = "final-writing-remediation"
pattern = "finding Data"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-f8cc133d2c1b]
kind = "requires"
slice = "final-writing-remediation"
pattern = "final remediation Work Unit"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-ac9dff940f63]
kind = "requires"
slice = "final-writing-remediation"
pattern = "`id`"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-aa7bc91d6a8b]
kind = "requires"
slice = "final-writing-remediation"
pattern = "`purpose`"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-eb0936c15816]
kind = "requires"
slice = "final-writing-remediation"
pattern = "`acceptance_criteria`"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-726f62ee839f]
kind = "requires"
slice = "final-writing-remediation"
pattern = "`scope`"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-7cd8ffcceff8]
kind = "requires"
slice = "final-writing-remediation"
pattern = "`change`"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-e1b93aad133e]
kind = "requires"
slice = "final-writing-remediation"
pattern = "`exclude`"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-aac4a2eb09bc]
kind = "requires"
slice = "final-writing-remediation"
pattern = "`implementation_freedom`"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-abee221073c9]
kind = "requires"
slice = "final-writing-remediation"
pattern = "`constraints`"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-562450be2674]
kind = "requires"
slice = "final-writing-remediation"
pattern = "`depends_on`"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-ee1e8ef3af7c]
kind = "requires"
slice = "final-writing-remediation"
pattern = "`verification`"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-ae4be56e2479]
kind = "requires"
slice = "final-writing-remediation"
pattern = "commit message"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-fc399aa1677a]
kind = "requires"
slice = "final-writing-remediation"
pattern = "commit range"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-3d02034ce816]
kind = "requires"
slice = "final-writing-remediation"
pattern = "before/after identity"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-0ffe47a66ac3]
kind = "requires"
slice = "final-writing-remediation"
pattern = "same-run accept exception"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-b93e9dbdec97]
kind = "requires"
slice = "final-writing-remediation"
pattern = "fresh Implementer context"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-4e3a0feb7633]
kind = "requires"
slice = "final-writing-remediation"
pattern = "single writer"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-7be6362fa4f0]
kind = "requires"
slice = "final-writing-remediation"
pattern = "mechanically restart"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-5fd5573f6598]
kind = "requires"
slice = "final-writing-remediation"
pattern = "mandatory final writing review"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-requires-ac5476bde199]
kind = "requires"
slice = "final-writing-remediation"
pattern = "closeout"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-forbids-a3252e730491]
kind = "forbids"
slice = "final-writing-remediation"
pattern = "review-patch-refactorer"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-forbids-dfc45d5af397]
kind = "forbids"
slice = "final-writing-remediation"
pattern = "fixed patch agent"
applies_to = ["claude", "codex"]

[contracts.final-writing-remediation-forbids-5e94eb370949]
kind = "forbids"
slice = "final-writing-remediation"
pattern = "全 finding の固定 loop"
applies_to = ["claude", "codex"]

[contracts.impl-risk-batch-loader-full-958687828fc2]
kind = "requires"
slice = "impl-risk-directed-batch-resolve-kernel-parent-mapping"
pattern = '''risk-directed review で最初の Resolution Transaction を開始する前に、親は生成後の skill directory から package-root reference へ
skill-relative `../../references/batch-resolve-kernel.md` を一度だけ読み、次の必要本文を検証する。

- identity は `batch-resolve-kernel-v1` である。
- Kernel dependencies は `none` である。
- `適用モデル`、`snapshot discipline`、`Resolution Transaction`、`caller boundary` の必要本文が揃っている。

reference の不足、identity または dependencies の不一致、必要本文の不足、読み取り失敗は推測で補わず
`stop-incomplete` へ返す。親が生成後の相対 path を解決して読み込み、reviewer に path 解決、reference の探索、読み込みを委ねない。
この loader は finding が0件の review、final writing gate だけの処理のためには起動しない。'''
applies_to = ["claude", "codex"]

[contracts.impl-risk-role-scope-e31d25c34f41]
kind = "requires"
slice = "impl-risk-directed-batch-resolve-kernel-parent-mapping"
pattern = '''この mapping の role は caller/resolver=`impl-lead parent`、counterpart=`risk-directed reviewer` とする。counterpart の finding は
transaction 外の non-binding Data であり、authority、既存の `adopted` / `rejected` / `unresolved`、return boundary、Work Unit / run
acceptance は親が保持する。'''
applies_to = ["claude", "codex"]

[contracts.impl-risk-snapshot-scope-8e5d9b548ceb]
kind = "requires"
slice = "impl-risk-directed-batch-resolve-kernel-parent-mapping"
pattern = '''`target_snapshot` は artifact-neutral な caller-owned candidate とし、必要な事前 verification を通過した immutable candidate を
`origin verified snapshot` として固定する。mapping は既存の Work Unit、AC、scope、exclude、責任境界を拡張しない。'''
applies_to = ["claude", "codex"]

[contracts.impl-risk-review-batch-621b177002ca]
kind = "requires"
slice = "impl-risk-directed-batch-resolve-kernel-parent-mapping"
pattern = '''reviewer の selection、invocation、observation、result collection、normalize、evidence確認は transaction 外で行う。親は review set を
counterpart invocation 前に固定し、必要な全 observation と result を回収し、finding を normalize して evidence を確認してから transaction を
開始する。必要な observation または result が一部でも欠けている場合は、暗黙に縮退して transaction を開始せず、既存の caller boundary または
`stop-incomplete` へ返す。1件以上の finding は、各 finding を `Resolution Point` へ mapping し、同じ `origin verified snapshot` の全 findings を一つの
`Resolution Batch` として固定する。finding が0件なら空の Resolution Transaction を開始せず、risk-directed review の結果を既存の親経路へ返す。'''
applies_to = ["claude", "codex"]

[contracts.impl-risk-adjudication-coherent-7123b7479517]
kind = "requires"
slice = "impl-risk-directed-batch-resolve-kernel-parent-mapping"
pattern = '''親は既存の `adopted` / `rejected` / `unresolved` を維持し、mutation 前に全 point を裁定する。全 point の裁定が終わるまで
mutation を開始せず、`adopted` は原則として一つの coherent remediation として扱う。`unresolved` と `adopted` が不可分なら mutation を行わず、
既存の caller-owned stop boundary へ返す。'''
applies_to = ["claude", "codex"]

[contracts.impl-risk-promotion-boundary-73f5e962a2a1]
kind = "requires"
slice = "impl-risk-directed-batch-resolve-kernel-parent-mapping"
pattern = '''verify と caller-owned semantic progress の後だけ promote し、updated snapshot の re-review は新しい Resolution Transaction とする。promotion
後の snapshot を risk-directed reviewer が再観測する場合は、新しい review set、Resolution Batch とする。Transaction
の closure は Work Unit や run の acceptance と別であり、closureだけで親のacceptを意味しない。'''
applies_to = ["claude", "codex"]

[contracts.impl-risk-final-writing-exclusion-c779a78c360d]
kind = "requires"
slice = "impl-risk-directed-batch-resolve-kernel-parent-mapping"
pattern = '''final writing gate はこの mapping の対象外であり、Kernel の partition、isolate、corrective adjudication を重複定義しない。mandatory final writing gate の
固有 loader、finding、remediation、verification は
既存の final writing reference と親責務に従う。'''
applies_to = ["claude", "codex"]

[contracts.impl-risk-final-writing-exclusion-occurrences]
kind = "occurrences"
pattern = "final writing gate はこの mapping の対象外であり、Kernel の partition、isolate、corrective adjudication を重複定義しない。"
count = 1
paths = [
  "plugins/claude/skills/impl-lead/SKILL.md",
  "plugins/codex/skills/impl-lead/SKILL.md",
]
applies_to = ["claude", "codex"]

[contracts.impl-risk-loader-before-transaction]
kind = "order"
before = "impl-risk-directed-batch-loader-start"
after = "impl-risk-directed-resolution-transaction-start"
applies_to = ["claude", "codex"]

[contracts.impl-risk-selection-before-loader]
kind = "order"
before = "impl-risk-selection-handoff-end"
after = "impl-risk-directed-batch-loader-start"
applies_to = ["claude", "codex"]

[contracts.impl-risk-loader-before-findings]
kind = "order"
before = "impl-risk-directed-resolution-transaction-start"
after = "impl-review-findings-start"
applies_to = ["claude", "codex"]
