[contracts.destination-selection-reference-identity]
kind = "occurrences"
pattern = "Destination selection identity: `destination-selection-v1`."
count = 1
paths = [
  "plugins/claude/references/destination-selection.md",
  "plugins/codex/references/destination-selection.md",
  "plugins/cursor/references/destination-selection.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-selection-not-kernel]
kind = "occurrences"
pattern = "Kernel ではない。Kernel identity と Kernel injection mapping を持たない。"
count = 1
paths = [
  "plugins/claude/references/destination-selection.md",
  "plugins/codex/references/destination-selection.md",
  "plugins/cursor/references/destination-selection.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-selection-no-kernel-identity]
kind = "forbids"
pattern = "Kernel identity:"
paths = [
  "plugins/claude/references/destination-selection.md",
  "plugins/codex/references/destination-selection.md",
  "plugins/cursor/references/destination-selection.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-selection-inputs]
kind = "occurrences"
pattern = '''storage_requirement.persistence = transient | persistent_reopenable
requested_destination?
runtime_candidates[]
caller_owned_predicates?'''
count = 1
paths = [
  "plugins/claude/references/destination-selection.md",
  "plugins/codex/references/destination-selection.md",
  "plugins/cursor/references/destination-selection.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-selection-no-plan-wayfind-enum]
kind = "forbids"
pattern = "plan | wayfind"
paths = [
  "plugins/claude/references/destination-selection.md",
  "plugins/codex/references/destination-selection.md",
  "plugins/cursor/references/destination-selection.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-selection-no-best-candidate]
kind = "forbids"
pattern = "best_candidate"
paths = [
  "plugins/claude/references/destination-selection.md",
  "plugins/codex/references/destination-selection.md",
  "plugins/cursor/references/destination-selection.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-selection-no-storage-adapter]
kind = "forbids"
pattern = "StorageAdapter"
paths = [
  "plugins/claude/references/destination-selection.md",
  "plugins/codex/references/destination-selection.md",
  "plugins/cursor/references/destination-selection.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-selection-no-tugite-auto-root]
kind = "occurrences"
pattern = "未指定時に独自 `.tugite/` を新設しない。"
count = 1
paths = [
  "plugins/claude/references/destination-selection.md",
  "plugins/codex/references/destination-selection.md",
  "plugins/cursor/references/destination-selection.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-selection-git-predicate-scope]
kind = "occurrences"
pattern = "適用しない: repository 外の verified OS-temp（資格は temp-root identity / containment / non-follow / exclusive creation intent）"
count = 1
paths = [
  "plugins/claude/references/destination-selection.md",
  "plugins/codex/references/destination-selection.md",
  "plugins/cursor/references/destination-selection.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-selection-wayfind-seam]
kind = "occurrences"
pattern = "初回 Map / Decision Unit canonical state 作成時に `persistent_reopenable` を渡す。"
count = 1
paths = [
  "plugins/claude/references/destination-selection.md",
  "plugins/codex/references/destination-selection.md",
  "plugins/cursor/references/destination-selection.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-selection-explicit-keep]
kind = "occurrences"
pattern = "元が Human explicit なら、同じ requested_destination を保持する。unique-best auto-select へ落とさない。"
count = 1
paths = [
  "plugins/claude/references/destination-selection.md",
  "plugins/codex/references/destination-selection.md",
  "plugins/cursor/references/destination-selection.md",
]
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-qualification-flow-0d13a2b8a564]
kind = "requires"
slice = "destination-qualification-flow"
pattern = "Trigger: 親が storage_requirement、optional requested_destination、runtime candidate facts、optional 適用範囲付き caller_owned_predicates を確定したとき。"
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-qualification-flow-77c4435c50d7]
kind = "requires"
slice = "destination-qualification-flow"
pattern = "Inputs: storage_requirement, requested_destination?, runtime_candidates, caller_owned_predicates?"
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-qualification-flow-490ab636b5eb]
kind = "requires"
slice = "destination-qualification-flow"
pattern = "unique comparison は Flow 内でしない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-qualification-flow-c3075d48c22c]
kind = "requires"
slice = "destination-qualification-flow"
pattern = "`transient` は verified OS-temp を qualify し得る。`persistent_reopenable` は ephemeral OS-temp を unqualified。"
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-qualification-flow-ae0698ec4ec4]
kind = "requires"
slice = "destination-qualification-flow"
pattern = "未指定時に directory / ignore rule を新設する candidate は作らない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-qualification-flow-e8240d0bd002]
kind = "requires"
slice = "destination-qualification-flow"
pattern = "Outcomes: explicit 確定、qualified set（未指定時）、explicit-destination incomplete、または入力不足 incomplete。"
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-qualification-no-unique-best-66d5d277ac44]
kind = "forbids"
slice = "destination-qualification-flow"
pattern = "一意な best だけ選ぶ"
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-qualification-flow-field-order]
kind = "order"
before = "dest-qual-trigger"
after = "dest-qual-inputs"
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-qualification-inputs-before-procedure]
kind = "order"
before = "dest-qual-inputs"
after = "dest-qual-procedure"
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-qualification-procedure-before-outcomes]
kind = "order"
before = "dest-qual-procedure"
after = "dest-qual-outcomes"
applies_to = ["claude", "codex", "cursor"]

[contracts.reopen-resolution-flow-c6d5a4bdf05f]
kind = "requires"
slice = "reopen-resolution-flow"
pattern = "Trigger: 親が既存 state の reopen_locator を確定し、resume を要求したとき。"
applies_to = ["claude", "codex", "cursor"]

[contracts.reopen-resolution-flow-e886d62436eb]
kind = "requires"
slice = "reopen-resolution-flow"
pattern = "Procedure: selector を再実行しない。compatible resolver で同一 state を解決する。失敗は relocation-required / incomplete。migration しない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.reopen-resolution-flow-f8541bbf2367]
kind = "requires"
slice = "reopen-resolution-flow"
pattern = "Outcomes: resolved destination、または relocation-required / incomplete。"
applies_to = ["claude", "codex", "cursor"]

[contracts.reopen-resolution-no-unique-best-431bad735b18]
kind = "forbids"
slice = "reopen-resolution-flow"
pattern = "一意な best だけ選ぶ"
applies_to = ["claude", "codex", "cursor"]

[contracts.reopen-resolution-flow-field-order]
kind = "order"
before = "reopen-trigger"
after = "reopen-inputs"
applies_to = ["claude", "codex", "cursor"]

[contracts.reopen-resolution-inputs-before-procedure]
kind = "order"
before = "reopen-inputs"
after = "reopen-procedure"
applies_to = ["claude", "codex", "cursor"]

[contracts.reopen-resolution-procedure-before-outcomes]
kind = "order"
before = "reopen-procedure"
after = "reopen-outcomes"
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-unique-selection-9a137285aef8]
kind = "requires"
slice = "destination-unique-selection"
pattern = "起動条件: qualification が qualified set を返したとき（explicit 確定、explicit incomplete、入力不足 incomplete では起動しない）。"
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-unique-selection-8df42a5e75ed]
kind = "requires"
slice = "destination-unique-selection"
pattern = "一意な best だけ選ぶ。同順位または比較不能は incomplete。OS-temp を同順位解消に使わない。qualified 0 は incomplete。"
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-unique-selection-21ccf28202f9]
kind = "requires"
slice = "destination-unique-selection"
pattern = "usage_evidence 付き project-local が複数で比較不能 → incomplete。共存する OS-temp へ進めない。"
applies_to = ["claude", "codex", "cursor"]

[contracts.destination-unique-selection-not-flow-b044fb39cdc4]
kind = "forbids"
slice = "destination-unique-selection"
pattern = "Trigger:"
applies_to = ["claude", "codex", "cursor"]
