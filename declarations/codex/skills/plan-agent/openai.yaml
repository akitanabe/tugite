interface:
  display_name: "Plan Agent"
  short_description: "Build a verified plan from Agent-side direction"
  default_prompt: "Use $plan-agent only when explicitly requested to establish Agent-side planning direction and return the common Planning Core result without starting implementation."
policy:
  allow_implicit_invocation: false
