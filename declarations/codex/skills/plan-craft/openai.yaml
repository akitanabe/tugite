interface:
  display_name: "Plan Craft"
  short_description: "Draft a free-form plan without starting implementation"
  default_prompt: "Use $plan-craft to draft or refine a free-form planning artifact without implementing, delegating, or advancing to a next stage."
policy:
  allow_implicit_invocation: false
