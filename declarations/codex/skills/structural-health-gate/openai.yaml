interface:
  display_name: "Internal Structural Health Gate"
  short_description: "Assess candidate structure and return evidence"
  default_prompt: "Use $structural-health-gate only inside an explicitly invoked proposal-family public workflow parent context; return evidence and assessment without editing, routing, or making the final decision."
policy:
  allow_implicit_invocation: true
