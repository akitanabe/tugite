interface:
  display_name: "Impl Lead"
  short_description: "Run bounded Work Unit loops with optional risk-directed review and a mandatory final writing gate"
  default_prompt: "Use $impl-lead to normalize one or more Work Units, select optional review for concrete decision-changing risk, complete the read-only final writing gate, and accept or stop-incomplete with evidence."
policy:
  allow_implicit_invocation: false
