interface:
  display_name: "Explorer This"
  short_description: "Run explicit exploration with one task-local model and direct requested output"
  default_prompt: "Use $explorer-this only for an explicit public exploration request; use one task-local Local Model, connect directly to the requested output, and qualified-stop when a material gap remains unresolved."
policy:
  allow_implicit_invocation: false
